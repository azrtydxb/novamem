module github.com/azrtydxb/novamem/clients/smoke

// The live smoke of every SDK against a server built from the PR: a
// schema check of the routes the SDKs rely on, the token minter and the
// embeddings stand-in. Standard library only.
go 1.23.0
