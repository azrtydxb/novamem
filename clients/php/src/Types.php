<?php

// Code generated by clients/gen. DO NOT EDIT.
// @codingStandardsIgnoreFile
//
// Wire types for the novamem API, generated from docs/api/openapi.json.
// Each object type is a readonly class built with named arguments, with
// toArray() (a JSON-ready array: unset and empty optional fields are left
// out, required ones are always present, timestamps are sent as UTC with a
// Z) and fromArray() (unknown keys are ignored, so a newer server still
// decodes). Enum values are plain strings, so an unknown one is kept.

declare(strict_types=1);

namespace Novamem\Types;

/** @internal Conversions shared by the generated types. */
final class Wire
{
    /** @param array{0: string, 1: mixed} $spec */
    public static function toWire(mixed $v, array $spec): mixed
    {
        [$kind, $arg] = $spec;
        if ($v === null) {
            return null;
        }
        return match ($kind) {
            'p' => $arg === 'datetime' ? self::timestamp($v) : ($arg === 'any' && $v === [] ? new \stdClass() : $v),
            'n' => is_object($v) && method_exists($v, 'toArray') ? $v->toArray() : $v,
            'a' => array_map(fn ($x) => self::toWire($x, $arg), (array) $v),
            // A map is a JSON object even when empty: json_encode([]) would
            // send [] and a strict server schema rejects it.
            default => (object) array_map(fn ($x) => self::toWire($x, $arg), (array) $v),
        };
    }

    /** @param array{0: string, 1: mixed} $spec */
    public static function fromWire(mixed $v, array $spec): mixed
    {
        [$kind, $arg] = $spec;
        if ($v === null) {
            return null;
        }
        return match ($kind) {
            'n' => is_array($v) && method_exists(__NAMESPACE__ . '\\' . $arg, 'fromArray')
                ? (__NAMESPACE__ . '\\' . $arg)::fromArray($v) : $v,
            'a' => is_array($v) ? array_map(fn ($x) => self::fromWire($x, $arg), $v) : $v,
            'm' => is_array($v) ? array_map(fn ($x) => self::fromWire($x, $arg), $v) : $v,
            default => $v,
        };
    }

    /**
     * A timestamp given as a DateTimeInterface or a string is sent as UTC
     * with a Z; a string that does not parse is sent unchanged, and the
     * server answers it.
     */
    public static function timestamp(mixed $v): mixed
    {
        try {
            $t = $v instanceof \DateTimeInterface ? \DateTimeImmutable::createFromInterface($v)
                : (is_string($v) ? new \DateTimeImmutable($v) : null);
        } catch (\Exception) {
            return $v;
        }
        return $t === null ? $v : $t->setTimezone(new \DateTimeZone('UTC'))->format('Y-m-d\TH:i:s.v\Z');
    }
}


final readonly class ActiveProject
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['active', 'active', ['n', 'ActiveProjectActive'], true],
    ];

    public function __construct(
        public ?ActiveProjectActive $active = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class ActiveProjectActive
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['id', 'id', ['p', 'string'], false],
        ['name', 'name', ['p', 'string'], false],
    ];

    public function __construct(
        public ?string $id = null,
        public ?string $name = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class AdminUser
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['createdAt', 'createdAt', ['p', 'string'], true],
        ['email', 'email', ['p', 'string'], true],
        ['entryCount', 'entryCount', ['p', 'int32'], true],
        ['id', 'id', ['p', 'string'], true],
        ['name', 'name', ['p', 'string'], true],
        ['role', 'role', ['p', 'string'], true],
        ['tokenCount', 'tokenCount', ['p', 'int32'], true],
    ];

    public function __construct(
        public string $createdAt,
        public string $email,
        public int $entryCount,
        public string $id,
        public string $role,
        public int $tokenCount,
        public ?string $name = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'createdAt' => '',
            'email' => '',
            'entryCount' => 0,
            'id' => '',
            'role' => '',
            'tokenCount' => 0,
        ];
    }
}

final readonly class AdminUserList
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['users', 'users', ['a', ['n', 'AdminUser']], true],
    ];

    public function __construct(
        public array $users,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'users' => [],
        ];
    }
}

final readonly class AdoptionReport
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['diagnostics', 'diagnostics', ['a', ['m', ['p', 'any']]], true],
        ['features', 'features', ['m', ['p', 'any']], false],
        ['mcp', 'mcp', ['m', ['p', 'any']], true],
        ['refresh', 'refresh', ['m', ['p', 'any']], false],
        ['requestedClient', 'requestedClient', ['p', 'string'], true],
        ['requiredTools', 'requiredTools', ['a', ['p', 'string']], false],
        ['server', 'server', ['m', ['p', 'any']], true],
    ];

    public function __construct(
        public array $diagnostics,
        public array $mcp,
        public string $requestedClient,
        public array $server,
        public ?array $features = null,
        public ?array $refresh = null,
        public ?array $requiredTools = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'diagnostics' => [],
            'mcp' => [],
            'requestedClient' => '',
            'server' => [],
        ];
    }
}

final readonly class CaptureRequest
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['agentName', 'agentName', ['p', 'string'], false],
        ['capturedFrom', 'capturedFrom', ['p', 'string'], false],
        ['confidence', 'confidence', ['p', 'float'], false],
        ['content', 'content', ['p', 'string'], true],
        ['expiresAt', 'expiresAt', ['p', 'datetime'], false],
        ['force', 'force', ['p', 'bool'], false],
        ['metadata', 'metadata', ['m', ['p', 'any']], false],
        ['namespace', 'namespace', ['p', 'string'], false],
        ['project', 'project', ['p', 'string'], false],
        ['sensitivity', 'sensitivity', ['n', 'CaptureRequestSensitivity'], false],
        ['source', 'source', ['p', 'string'], false],
        ['sourceType', 'sourceType', ['p', 'string'], false],
    ];

    public function __construct(
        public string $content,
        public ?string $agentName = null,
        public ?string $capturedFrom = null,
        public ?float $confidence = null,
        public \DateTimeInterface|string|null $expiresAt = null,
        public ?bool $force = null,
        public ?array $metadata = null,
        public ?string $namespace = null,
        public ?string $project = null,
        public ?string $sensitivity = null,
        public ?string $source = null,
        public ?string $sourceType = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'content' => '',
        ];
    }
}

/** Values of CaptureRequestSensitivity. Unknown values from a newer server are kept as strings. */
final class CaptureRequestSensitivity
{
    public const PUBLIC = 'public';
    public const INTERNAL = 'internal';
    public const PRIVATE = 'private';
    public const SENSITIVE = 'sensitive';
}

final readonly class CaptureResult
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['deduplicated', 'deduplicated', ['p', 'bool'], false],
        ['embedded', 'embedded', ['p', 'bool'], false],
        ['id', 'id', ['p', 'string'], true],
        ['rejected', 'rejected', ['p', 'string'], false],
        ['superseded', 'superseded', ['a', ['p', 'string']], false],
        ['updated', 'updated', ['p', 'bool'], false],
    ];

    public function __construct(
        public ?bool $deduplicated = null,
        public ?bool $embedded = null,
        public ?string $id = null,
        public ?string $rejected = null,
        public ?array $superseded = null,
        public ?bool $updated = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class ChangeFeed
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['changes', 'changes', ['a', ['n', 'ChangeFeedChangesItem']], true],
        ['nextSeq', 'nextSeq', ['p', 'int64'], false],
    ];

    public function __construct(
        public array $changes,
        public ?int $nextSeq = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'changes' => [],
        ];
    }
}

final readonly class ChangeFeedChangesItem
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['at', 'at', ['p', 'string'], false],
        ['change', 'change', ['n', 'ChangeFeedChangesItemChange'], false],
        ['detail', 'detail', ['m', ['p', 'any']], false],
        ['entryId', 'entryId', ['p', 'string'], false],
        ['projectId', 'projectId', ['p', 'string'], false],
        ['seq', 'seq', ['p', 'int64'], false],
    ];

    public function __construct(
        public ?string $at = null,
        public ?string $change = null,
        public ?array $detail = null,
        public ?string $entryId = null,
        public ?string $projectId = null,
        public ?int $seq = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

/** Values of ChangeFeedChangesItemChange. Unknown values from a newer server are kept as strings. */
final class ChangeFeedChangesItemChange
{
    public const CREATED = 'created';
    public const UPDATED = 'updated';
    public const SUPERSEDED = 'superseded';
    public const DELETED = 'deleted';
    public const EXPIRED = 'expired';
}

final readonly class ContextPrefix
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['prefix', 'prefix', ['p', 'string'], true],
    ];

    public function __construct(
        public string $prefix,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'prefix' => '',
        ];
    }
}

final readonly class ContextRequest
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['asOf', 'asOf', ['p', 'datetime'], false],
        ['decompose', 'decompose', ['p', 'bool'], false],
        ['expandSourceChunks', 'expandSourceChunks', ['p', 'bool'], false],
        ['includeNamespaces', 'includeNamespaces', ['a', ['p', 'string']], false],
        ['includeProjects', 'includeProjects', ['a', ['p', 'string']], false],
        ['k', 'k', ['p', 'int32'], false],
        ['maxSensitivity', 'maxSensitivity', ['n', 'ContextRequestMaxSensitivity'], false],
        ['maxTokens', 'maxTokens', ['p', 'int32'], false],
        ['message', 'message', ['p', 'string'], true],
        ['namespace', 'namespace', ['p', 'string'], false],
        ['project', 'project', ['p', 'string'], false],
        ['weights', 'weights', ['n', 'ContextRequestWeights'], false],
    ];

    public function __construct(
        public string $message,
        public \DateTimeInterface|string|null $asOf = null,
        public ?bool $decompose = null,
        public ?bool $expandSourceChunks = null,
        public ?array $includeNamespaces = null,
        public ?array $includeProjects = null,
        public ?int $k = null,
        public ?string $maxSensitivity = null,
        public ?int $maxTokens = null,
        public ?string $namespace = null,
        public ?string $project = null,
        public ?ContextRequestWeights $weights = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'message' => '',
        ];
    }
}

/** Values of ContextRequestMaxSensitivity. Unknown values from a newer server are kept as strings. */
final class ContextRequestMaxSensitivity
{
    public const PUBLIC = 'public';
    public const INTERNAL = 'internal';
    public const PRIVATE = 'private';
    public const SENSITIVE = 'sensitive';
}

final readonly class ContextRequestWeights
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['entity', 'entity', ['p', 'float'], false],
        ['graph', 'graph', ['p', 'float'], false],
        ['keyword', 'keyword', ['p', 'float'], false],
        ['recency', 'recency', ['p', 'float'], false],
        ['vector', 'vector', ['p', 'float'], false],
    ];

    public function __construct(
        public ?float $entity = null,
        public ?float $graph = null,
        public ?float $keyword = null,
        public ?float $recency = null,
        public ?float $vector = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class DecayResult
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['demoted', 'demoted', ['p', 'float'], true],
        ['expired', 'expired', ['p', 'float'], true],
        ['promoted', 'promoted', ['p', 'float'], true],
    ];

    public function __construct(
        public float $demoted,
        public float $expired,
        public float $promoted,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'demoted' => 0.0,
            'expired' => 0.0,
            'promoted' => 0.0,
        ];
    }
}

final readonly class EntryList
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['results', 'results', ['a', ['n', 'MemoryEntry']], true],
    ];

    public function __construct(
        public array $results,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'results' => [],
        ];
    }
}

final readonly class EvaluateReport
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['cases', 'cases', ['a', ['n', 'EvaluateReportCasesItem']], true],
        ['passed', 'passed', ['p', 'bool'], true],
        ['suite', 'suite', ['p', 'string'], true],
        ['summary', 'summary', ['n', 'EvaluateReportSummary'], true],
    ];

    public function __construct(
        public array $cases,
        public bool $passed,
        public string $suite,
        public EvaluateReportSummary $summary,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'cases' => [],
            'passed' => false,
            'suite' => '',
            'summary' => EvaluateReportSummary::fromArray([]),
        ];
    }
}

final readonly class EvaluateReportCasesItem
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['name', 'name', ['p', 'string'], false],
        ['passed', 'passed', ['p', 'bool'], false],
    ];

    public function __construct(
        public ?string $name = null,
        public ?bool $passed = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class EvaluateReportSummary
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['failed', 'failed', ['p', 'float'], false],
        ['passed', 'passed', ['p', 'float'], false],
        ['total', 'total', ['p', 'float'], false],
    ];

    public function __construct(
        public ?float $failed = null,
        public ?float $passed = null,
        public ?float $total = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class ExportPage
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['entries', 'entries', ['a', ['n', 'ExportedEntry']], true],
        ['nextAfterId', 'nextAfterId', ['p', 'string'], true],
    ];

    public function __construct(
        public array $entries,
        public ?string $nextAfterId = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'entries' => [],
        ];
    }
}

final readonly class ExportedEntry
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['agentName', 'agentName', ['p', 'string'], true],
        ['capturedFrom', 'capturedFrom', ['p', 'string'], true],
        ['confidence', 'confidence', ['p', 'float'], true],
        ['content', 'content', ['p', 'string'], true],
        ['createdAt', 'createdAt', ['p', 'string'], true],
        ['id', 'id', ['p', 'string'], true],
        ['metadata', 'metadata', ['m', ['p', 'any']], true],
        ['namespace', 'namespace', ['p', 'string'], true],
        ['projectId', 'projectId', ['p', 'string'], true],
        ['source', 'source', ['p', 'string'], true],
        ['sourceType', 'sourceType', ['p', 'string'], true],
        ['updatedAt', 'updatedAt', ['p', 'string'], true],
    ];

    public function __construct(
        public float $confidence,
        public string $content,
        public string $createdAt,
        public string $id,
        public array $metadata,
        public string $namespace,
        public string $source,
        public string $updatedAt,
        public ?string $agentName = null,
        public ?string $capturedFrom = null,
        public ?string $projectId = null,
        public ?string $sourceType = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'confidence' => 0.0,
            'content' => '',
            'createdAt' => '',
            'id' => '',
            'metadata' => [],
            'namespace' => '',
            'source' => '',
            'updatedAt' => '',
        ];
    }
}

final readonly class ForgetRequest
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['id', 'id', ['p', 'string'], true],
        ['project', 'project', ['p', 'string'], false],
    ];

    public function __construct(
        public string $id,
        public ?string $project = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'id' => '',
        ];
    }
}

final readonly class ForgetResult
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['coldDeleteOk', 'coldDeleteOk', ['p', 'bool'], false],
        ['deleted', 'deleted', ['p', 'bool'], true],
    ];

    public function __construct(
        public bool $deleted,
        public ?bool $coldDeleteOk = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'deleted' => false,
        ];
    }
}

final readonly class Health
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['ok', 'ok', ['p', 'bool'], true],
    ];

    public function __construct(
        public bool $ok,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'ok' => false,
        ];
    }
}

final readonly class HygieneReport
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['contradictionCandidates', 'contradictionCandidates', ['a', ['p', 'any']], false],
        ['duplicateClusters', 'duplicateClusters', ['a', ['p', 'any']], false],
        ['lowValue', 'lowValue', ['a', ['p', 'any']], false],
        ['orphanCandidates', 'orphanCandidates', ['a', ['p', 'any']], false],
        ['stale', 'stale', ['a', ['p', 'any']], false],
        ['summary', 'summary', ['n', 'HygieneReportSummary'], true],
    ];

    public function __construct(
        public HygieneReportSummary $summary,
        public ?array $contradictionCandidates = null,
        public ?array $duplicateClusters = null,
        public ?array $lowValue = null,
        public ?array $orphanCandidates = null,
        public ?array $stale = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'summary' => HygieneReportSummary::fromArray([]),
        ];
    }
}

final readonly class HygieneReportSummary
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['contradictionCandidates', 'contradictionCandidates', ['p', 'float'], false],
        ['duplicateClusters', 'duplicateClusters', ['p', 'float'], false],
        ['lowValue', 'lowValue', ['p', 'float'], false],
        ['orphanCandidates', 'orphanCandidates', ['p', 'float'], false],
        ['scanned', 'scanned', ['p', 'float'], false],
        ['stale', 'stale', ['p', 'float'], false],
    ];

    public function __construct(
        public ?float $contradictionCandidates = null,
        public ?float $duplicateClusters = null,
        public ?float $lowValue = null,
        public ?float $orphanCandidates = null,
        public ?float $scanned = null,
        public ?float $stale = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class ImportResult
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['deduplicated', 'deduplicated', ['p', 'int32'], true],
        ['failed', 'failed', ['a', ['n', 'ImportResultFailedItem']], true],
        ['imported', 'imported', ['p', 'int32'], true],
    ];

    public function __construct(
        public int $deduplicated,
        public array $failed,
        public int $imported,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'deduplicated' => 0,
            'failed' => [],
            'imported' => 0,
        ];
    }
}

final readonly class ImportResultFailedItem
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['error', 'error', ['p', 'string'], true],
        ['index', 'index', ['p', 'int32'], true],
    ];

    public function __construct(
        public string $error,
        public int $index,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'error' => '',
            'index' => 0,
        ];
    }
}

final readonly class MemberAdded
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['added', 'added', ['p', 'bool'], true],
        ['userId', 'userId', ['p', 'string'], true],
        ['username', 'username', ['p', 'string'], true],
    ];

    public function __construct(
        public bool $added,
        public string $userId,
        public string $username,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'added' => false,
            'userId' => '',
            'username' => '',
        ];
    }
}

final readonly class MemberList
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['members', 'members', ['a', ['n', 'MemberListMembersItem']], true],
    ];

    public function __construct(
        public array $members,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'members' => [],
        ];
    }
}

final readonly class MemberListMembersItem
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['joinedAt', 'joinedAt', ['p', 'string'], false],
        ['role', 'role', ['p', 'string'], false],
        ['userId', 'userId', ['p', 'string'], false],
        ['username', 'username', ['p', 'string'], false],
    ];

    public function __construct(
        public ?string $joinedAt = null,
        public ?string $role = null,
        public ?string $userId = null,
        public ?string $username = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class MemberRemoved
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['removed', 'removed', ['p', 'bool'], true],
    ];

    public function __construct(
        public bool $removed,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'removed' => false,
        ];
    }
}

final readonly class MemoryEntry
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['content', 'content', ['p', 'string'], true],
        ['id', 'id', ['p', 'string'], true],
        ['metadata', 'metadata', ['m', ['p', 'any']], false],
        ['namespace', 'namespace', ['p', 'string'], false],
        ['project', 'project', ['p', 'string'], false],
        ['score', 'score', ['p', 'float'], false],
        ['signals', 'signals', ['n', 'MemoryEntrySignals'], false],
        ['source', 'source', ['p', 'string'], false],
        ['tier', 'tier', ['n', 'MemoryEntryTier'], false],
    ];

    public function __construct(
        public string $content,
        public string $id,
        public ?array $metadata = null,
        public ?string $namespace = null,
        public ?string $project = null,
        public ?float $score = null,
        public ?MemoryEntrySignals $signals = null,
        public ?string $source = null,
        public ?string $tier = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'content' => '',
            'id' => '',
        ];
    }
}

final readonly class MemoryEntrySignals
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['entity', 'entity', ['p', 'float'], false],
        ['graph', 'graph', ['p', 'float'], false],
        ['keyword', 'keyword', ['p', 'float'], false],
        ['recency', 'recency', ['p', 'float'], false],
        ['vector', 'vector', ['p', 'float'], false],
    ];

    public function __construct(
        public ?float $entity = null,
        public ?float $graph = null,
        public ?float $keyword = null,
        public ?float $recency = null,
        public ?float $vector = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

/** Values of MemoryEntryTier. Unknown values from a newer server are kept as strings. */
final class MemoryEntryTier
{
    public const WARM = 'warm';
    public const COLD = 'cold';
}

final readonly class MintTokenRequest
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['expiresInDays', 'expiresInDays', ['p', 'int32'], false],
        ['label', 'label', ['p', 'string'], false],
        ['project', 'project', ['p', 'string'], false],
        ['scope', 'scope', ['n', 'MintTokenRequestScope'], false],
    ];

    public function __construct(
        public ?int $expiresInDays = null,
        public ?string $label = null,
        public ?string $project = null,
        public ?string $scope = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

/** Values of MintTokenRequestScope. Unknown values from a newer server are kept as strings. */
final class MintTokenRequestScope
{
    public const FULL = 'full';
    public const READ_ONLY = 'read_only';
}

final readonly class MintedToken
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['createdAt', 'createdAt', ['p', 'string'], false],
        ['expiresAt', 'expiresAt', ['p', 'string'], false],
        ['projectId', 'projectId', ['p', 'string'], false],
        ['scope', 'scope', ['p', 'string'], false],
        ['token', 'token', ['p', 'string'], true],
        ['userId', 'userId', ['p', 'string'], false],
        ['warning', 'warning', ['p', 'string'], false],
    ];

    public function __construct(
        public string $token,
        public ?string $createdAt = null,
        public ?string $expiresAt = null,
        public ?string $projectId = null,
        public ?string $scope = null,
        public ?string $userId = null,
        public ?string $warning = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'token' => '',
        ];
    }
}

final readonly class NeighborsRequest
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['depth', 'depth', ['p', 'int32'], false],
        ['id', 'id', ['p', 'string'], true],
        ['includeNamespaces', 'includeNamespaces', ['a', ['p', 'string']], false],
        ['includeProjects', 'includeProjects', ['a', ['p', 'string']], false],
        ['k', 'k', ['p', 'int32'], false],
        ['maxSensitivity', 'maxSensitivity', ['n', 'NeighborsRequestMaxSensitivity'], false],
        ['project', 'project', ['p', 'string'], false],
    ];

    public function __construct(
        public string $id,
        public ?int $depth = null,
        public ?array $includeNamespaces = null,
        public ?array $includeProjects = null,
        public ?int $k = null,
        public ?string $maxSensitivity = null,
        public ?string $project = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'id' => '',
        ];
    }
}

/** Values of NeighborsRequestMaxSensitivity. Unknown values from a newer server are kept as strings. */
final class NeighborsRequestMaxSensitivity
{
    public const PUBLIC = 'public';
    public const INTERNAL = 'internal';
    public const PRIVATE = 'private';
    public const SENSITIVE = 'sensitive';
}

final readonly class ObserveResult
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['logChars', 'logChars', ['p', 'float'], false],
        ['observed', 'observed', ['p', 'float'], false],
        ['reflected', 'reflected', ['p', 'bool'], false],
    ];

    public function __construct(
        public ?float $logChars = null,
        public ?float $observed = null,
        public ?bool $reflected = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class Project
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['createdAt', 'createdAt', ['p', 'string'], false],
        ['id', 'id', ['p', 'string'], true],
        ['name', 'name', ['p', 'string'], true],
        ['ownerUserId', 'ownerUserId', ['p', 'string'], false],
    ];

    public function __construct(
        public string $id,
        public string $name,
        public ?string $createdAt = null,
        public ?string $ownerUserId = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'id' => '',
            'name' => '',
        ];
    }
}

final readonly class ProjectDeleted
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['coldCollectionsDropped', 'coldCollectionsDropped', ['a', ['p', 'string']], false],
        ['deleted', 'deleted', ['p', 'bool'], true],
        ['entriesRemoved', 'entriesRemoved', ['p', 'float'], false],
        ['graphCleared', 'graphCleared', ['p', 'bool'], false],
    ];

    public function __construct(
        public bool $deleted,
        public ?array $coldCollectionsDropped = null,
        public ?float $entriesRemoved = null,
        public ?bool $graphCleared = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'deleted' => false,
        ];
    }
}

final readonly class ProjectList
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['projects', 'projects', ['a', ['n', 'ProjectListItem']], true],
    ];

    public function __construct(
        public array $projects,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'projects' => [],
        ];
    }
}

final readonly class ProjectListItem
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['createdAt', 'createdAt', ['p', 'string'], false],
        ['id', 'id', ['p', 'string'], true],
        ['name', 'name', ['p', 'string'], true],
        ['ownerUserId', 'ownerUserId', ['p', 'string'], false],
        ['role', 'role', ['p', 'string'], true],
    ];

    public function __construct(
        public string $id,
        public string $name,
        public string $role,
        public ?string $createdAt = null,
        public ?string $ownerUserId = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'id' => '',
            'name' => '',
            'role' => '',
        ];
    }
}

final readonly class ProvisionUserRequest
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['email', 'email', ['p', 'string'], true],
        ['name', 'name', ['p', 'string'], false],
        ['password', 'password', ['p', 'string'], true],
        ['tokenExpiresInDays', 'tokenExpiresInDays', ['p', 'int32'], false],
        ['tokenLabel', 'tokenLabel', ['p', 'string'], false],
        ['tokenScope', 'tokenScope', ['n', 'ProvisionUserRequestTokenScope'], false],
    ];

    public function __construct(
        public string $email,
        public string $password,
        public ?string $name = null,
        public ?int $tokenExpiresInDays = null,
        public ?string $tokenLabel = null,
        public ?string $tokenScope = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'email' => '',
            'password' => '',
        ];
    }
}

/** Values of ProvisionUserRequestTokenScope. Unknown values from a newer server are kept as strings. */
final class ProvisionUserRequestTokenScope
{
    public const FULL = 'full';
    public const READ_ONLY = 'read_only';
}

final readonly class ProvisionedUser
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['email', 'email', ['p', 'string'], true],
        ['token', 'token', ['p', 'string'], false],
        ['userId', 'userId', ['p', 'string'], true],
    ];

    public function __construct(
        public string $email,
        public string $userId,
        public ?string $token = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'email' => '',
            'userId' => '',
        ];
    }
}

final readonly class QuotaResult
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['quota', 'quota', ['n', 'QuotaResultQuota'], true],
        ['userId', 'userId', ['p', 'string'], true],
    ];

    public function __construct(
        public QuotaResultQuota $quota,
        public string $userId,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'quota' => QuotaResultQuota::fromArray([]),
            'userId' => '',
        ];
    }
}

final readonly class QuotaResultQuota
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['maxEntries', 'maxEntries', ['p', 'int32'], true],
        ['writesPerMinute', 'writesPerMinute', ['p', 'int32'], true],
    ];

    public function __construct(
        public ?int $maxEntries = null,
        public ?int $writesPerMinute = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class RecentRequest
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['contentMode', 'contentMode', ['n', 'RecentRequestContentMode'], false],
        ['includeNamespaces', 'includeNamespaces', ['a', ['p', 'string']], false],
        ['includeProjects', 'includeProjects', ['a', ['p', 'string']], false],
        ['k', 'k', ['p', 'int32'], false],
        ['maxSensitivity', 'maxSensitivity', ['n', 'RecentRequestMaxSensitivity'], false],
        ['namespace', 'namespace', ['p', 'string'], false],
        ['project', 'project', ['p', 'string'], false],
        ['since', 'since', ['p', 'datetime'], false],
    ];

    public function __construct(
        public ?string $contentMode = null,
        public ?array $includeNamespaces = null,
        public ?array $includeProjects = null,
        public ?int $k = null,
        public ?string $maxSensitivity = null,
        public ?string $namespace = null,
        public ?string $project = null,
        public \DateTimeInterface|string|null $since = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

/** Values of RecentRequestContentMode. Unknown values from a newer server are kept as strings. */
final class RecentRequestContentMode
{
    public const FULL = 'full';
    public const SNIPPET = 'snippet';
    public const IDS = 'ids';
}

/** Values of RecentRequestMaxSensitivity. Unknown values from a newer server are kept as strings. */
final class RecentRequestMaxSensitivity
{
    public const PUBLIC = 'public';
    public const INTERNAL = 'internal';
    public const PRIVATE = 'private';
    public const SENSITIVE = 'sensitive';
}

final readonly class RememberResult
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['deduplicated', 'deduplicated', ['p', 'bool'], false],
        ['embedded', 'embedded', ['p', 'bool'], false],
        ['id', 'id', ['p', 'string'], true],
        ['rejected', 'rejected', ['p', 'string'], false],
        ['superseded', 'superseded', ['a', ['p', 'string']], false],
        ['updated', 'updated', ['p', 'bool'], false],
    ];

    public function __construct(
        public ?bool $deduplicated = null,
        public ?bool $embedded = null,
        public ?string $id = null,
        public ?string $rejected = null,
        public ?array $superseded = null,
        public ?bool $updated = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class RevokeResult
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['revoked', 'revoked', ['p', 'bool'], true],
    ];

    public function __construct(
        public bool $revoked,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'revoked' => false,
        ];
    }
}

final readonly class SearchRequest
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['agentName', 'agentName', ['p', 'string'], false],
        ['asOf', 'asOf', ['p', 'datetime'], false],
        ['contentMode', 'contentMode', ['n', 'SearchRequestContentMode'], false],
        ['decompose', 'decompose', ['p', 'bool'], false],
        ['expandSourceChunks', 'expandSourceChunks', ['p', 'bool'], false],
        ['includeNamespaces', 'includeNamespaces', ['a', ['p', 'string']], false],
        ['includeProjects', 'includeProjects', ['a', ['p', 'string']], false],
        ['k', 'k', ['p', 'int32'], false],
        ['maxSensitivity', 'maxSensitivity', ['n', 'SearchRequestMaxSensitivity'], false],
        ['maxTokens', 'maxTokens', ['p', 'int32'], false],
        ['minVectorScore', 'minVectorScore', ['p', 'float'], false],
        ['namespace', 'namespace', ['p', 'string'], false],
        ['project', 'project', ['p', 'string'], false],
        ['query', 'query', ['p', 'string'], true],
        ['rerank', 'rerank', ['p', 'bool'], false],
        ['weights', 'weights', ['n', 'SearchRequestWeights'], false],
    ];

    public function __construct(
        public string $query,
        public ?string $agentName = null,
        public \DateTimeInterface|string|null $asOf = null,
        public ?string $contentMode = null,
        public ?bool $decompose = null,
        public ?bool $expandSourceChunks = null,
        public ?array $includeNamespaces = null,
        public ?array $includeProjects = null,
        public ?int $k = null,
        public ?string $maxSensitivity = null,
        public ?int $maxTokens = null,
        public ?float $minVectorScore = null,
        public ?string $namespace = null,
        public ?string $project = null,
        public ?bool $rerank = null,
        public ?SearchRequestWeights $weights = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'query' => '',
        ];
    }
}

/** Values of SearchRequestContentMode. Unknown values from a newer server are kept as strings. */
final class SearchRequestContentMode
{
    public const FULL = 'full';
    public const SNIPPET = 'snippet';
    public const IDS = 'ids';
}

/** Values of SearchRequestMaxSensitivity. Unknown values from a newer server are kept as strings. */
final class SearchRequestMaxSensitivity
{
    public const PUBLIC = 'public';
    public const INTERNAL = 'internal';
    public const PRIVATE = 'private';
    public const SENSITIVE = 'sensitive';
}

final readonly class SearchRequestWeights
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['entity', 'entity', ['p', 'float'], false],
        ['graph', 'graph', ['p', 'float'], false],
        ['keyword', 'keyword', ['p', 'float'], false],
        ['recency', 'recency', ['p', 'float'], false],
        ['vector', 'vector', ['p', 'float'], false],
    ];

    public function __construct(
        public ?float $entity = null,
        public ?float $graph = null,
        public ?float $keyword = null,
        public ?float $recency = null,
        public ?float $vector = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class SearchResult
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['degraded', 'degraded', ['p', 'bool'], false],
        ['results', 'results', ['a', ['n', 'MemoryEntry']], true],
    ];

    public function __construct(
        public array $results,
        public ?bool $degraded = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'results' => [],
        ];
    }
}

final readonly class SessionRecapRequest
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['agentName', 'agentName', ['p', 'string'], false],
        ['capturedFrom', 'capturedFrom', ['p', 'string'], false],
        ['confidence', 'confidence', ['p', 'float'], false],
        ['decisions', 'decisions', ['a', ['p', 'string']], false],
        ['force', 'force', ['p', 'bool'], false],
        ['metadata', 'metadata', ['m', ['p', 'any']], false],
        ['namespace', 'namespace', ['p', 'string'], false],
        ['other', 'other', ['a', ['p', 'string']], false],
        ['preferences', 'preferences', ['a', ['p', 'string']], false],
        ['project', 'project', ['p', 'string'], false],
        ['projectConventions', 'projectConventions', ['a', ['p', 'string']], false],
        ['rootCauses', 'rootCauses', ['a', ['p', 'string']], false],
        ['safetyConstraints', 'safetyConstraints', ['a', ['p', 'string']], false],
        ['sensitivity', 'sensitivity', ['n', 'SessionRecapRequestSensitivity'], false],
        ['setupFacts', 'setupFacts', ['a', ['p', 'string']], false],
        ['source', 'source', ['p', 'string'], false],
        ['sourceType', 'sourceType', ['p', 'string'], false],
    ];

    public function __construct(
        public ?string $agentName = null,
        public ?string $capturedFrom = null,
        public ?float $confidence = null,
        public ?array $decisions = null,
        public ?bool $force = null,
        public ?array $metadata = null,
        public ?string $namespace = null,
        public ?array $other = null,
        public ?array $preferences = null,
        public ?string $project = null,
        public ?array $projectConventions = null,
        public ?array $rootCauses = null,
        public ?array $safetyConstraints = null,
        public ?string $sensitivity = null,
        public ?array $setupFacts = null,
        public ?string $source = null,
        public ?string $sourceType = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

/** Values of SessionRecapRequestSensitivity. Unknown values from a newer server are kept as strings. */
final class SessionRecapRequestSensitivity
{
    public const PUBLIC = 'public';
    public const INTERNAL = 'internal';
    public const PRIVATE = 'private';
    public const SENSITIVE = 'sensitive';
}

final readonly class SessionRecapResult
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['results', 'results', ['a', ['n', 'CaptureResult']], true],
        ['saved', 'saved', ['p', 'float'], true],
    ];

    public function __construct(
        public array $results,
        public float $saved,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'results' => [],
            'saved' => 0.0,
        ];
    }
}

final readonly class Stats
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['byNamespace', 'byNamespace', ['m', ['n', 'StatsByNamespaceValue']], true],
        ['lastDecayAt', 'lastDecayAt', ['p', 'string'], false],
        ['totalCold', 'totalCold', ['p', 'float'], true],
        ['totalWarm', 'totalWarm', ['p', 'float'], true],
        ['uptimeMs', 'uptimeMs', ['p', 'float'], false],
    ];

    public function __construct(
        public array $byNamespace,
        public float $totalCold,
        public float $totalWarm,
        public ?string $lastDecayAt = null,
        public ?float $uptimeMs = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'byNamespace' => [],
            'totalCold' => 0.0,
            'totalWarm' => 0.0,
        ];
    }
}

final readonly class StatsByNamespaceValue
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['cold', 'cold', ['p', 'float'], false],
        ['warm', 'warm', ['p', 'float'], false],
    ];

    public function __construct(
        public ?float $cold = null,
        public ?float $warm = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class TokenDeleted
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['deleted', 'deleted', ['p', 'bool'], true],
    ];

    public function __construct(
        public bool $deleted,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'deleted' => false,
        ];
    }
}

final readonly class TokenList
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['tokens', 'tokens', ['a', ['m', ['p', 'any']]], true],
    ];

    public function __construct(
        public array $tokens,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'tokens' => [],
        ];
    }
}

final readonly class UpdateRequest
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['capturedFrom', 'capturedFrom', ['p', 'string'], false],
        ['confidence', 'confidence', ['p', 'float'], false],
        ['content', 'content', ['p', 'string'], false],
        ['metadata', 'metadata', ['m', ['p', 'any']], false],
        ['namespace', 'namespace', ['p', 'string'], false],
        ['project', 'project', ['p', 'string'], false],
        ['sensitivity', 'sensitivity', ['n', 'UpdateRequestSensitivity'], false],
        ['sourceType', 'sourceType', ['p', 'string'], false],
    ];

    public function __construct(
        public ?string $capturedFrom = null,
        public ?float $confidence = null,
        public ?string $content = null,
        public ?array $metadata = null,
        public ?string $namespace = null,
        public ?string $project = null,
        public ?string $sensitivity = null,
        public ?string $sourceType = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

/** Values of UpdateRequestSensitivity. Unknown values from a newer server are kept as strings. */
final class UpdateRequestSensitivity
{
    public const PUBLIC = 'public';
    public const INTERNAL = 'internal';
    public const PRIVATE = 'private';
    public const SENSITIVE = 'sensitive';
}

final readonly class UpdateResult
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['embeddingChanged', 'embeddingChanged', ['p', 'bool'], true],
        ['id', 'id', ['p', 'string'], true],
        ['updated', 'updated', ['p', 'bool'], true],
    ];

    public function __construct(
        public bool $embeddingChanged,
        public string $id,
        public bool $updated,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'embeddingChanged' => false,
            'id' => '',
            'updated' => false,
        ];
    }
}

final readonly class Usage
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['entries', 'entries', ['p', 'float'], false],
        ['quota', 'quota', ['n', 'UsageQuota'], false],
    ];

    public function __construct(
        public ?float $entries = null,
        public ?UsageQuota $quota = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class UsageQuota
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['maxEntries', 'maxEntries', ['p', 'float'], false],
        ['writesPerMinute', 'writesPerMinute', ['p', 'float'], false],
    ];

    public function __construct(
        public ?float $maxEntries = null,
        public ?float $writesPerMinute = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
        ];
    }
}

final readonly class UserDeletion
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['coldCleanup', 'coldCleanup', ['a', ['p', 'string']], true],
        ['coldCleanupOk', 'coldCleanupOk', ['p', 'bool'], true],
        ['deleted', 'deleted', ['p', 'bool'], true],
        ['entriesRemoved', 'entriesRemoved', ['p', 'int32'], true],
        ['projectsDeleted', 'projectsDeleted', ['a', ['p', 'string']], true],
        ['tokensRemoved', 'tokensRemoved', ['p', 'int32'], true],
    ];

    public function __construct(
        public array $coldCleanup,
        public bool $coldCleanupOk,
        public bool $deleted,
        public int $entriesRemoved,
        public array $projectsDeleted,
        public int $tokensRemoved,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'coldCleanup' => [],
            'coldCleanupOk' => false,
            'deleted' => false,
            'entriesRemoved' => 0,
            'projectsDeleted' => [],
            'tokensRemoved' => 0,
        ];
    }
}

final readonly class UserDeletionPreview
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['dryRun', 'dryRun', ['p', 'bool'], true],
        ['wouldDelete', 'wouldDelete', ['n', 'UserDeletionPreviewWouldDelete'], true],
    ];

    public function __construct(
        public bool $dryRun,
        public UserDeletionPreviewWouldDelete $wouldDelete,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'dryRun' => false,
            'wouldDelete' => UserDeletionPreviewWouldDelete::fromArray([]),
        ];
    }
}

final readonly class UserDeletionPreviewWouldDelete
{
    /** @var list<array{0: string, 1: string, 2: array<int, mixed>, 3: bool}> */
    private const FIELDS = [
        ['email', 'email', ['p', 'string'], false],
        ['entries', 'entries', ['p', 'int32'], true],
        ['ownedProjects', 'ownedProjects', ['a', ['p', 'string']], true],
        ['tokens', 'tokens', ['p', 'int32'], true],
        ['userId', 'userId', ['p', 'string'], true],
    ];

    public function __construct(
        public int $entries,
        public array $ownedProjects,
        public int $tokens,
        public string $userId,
        public ?string $email = null,
    ) {
    }

    /** @param array<string, mixed> $a */
    public static function fromArray(array $a): self
    {
        $args = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = Wire::fromWire($a[$wire] ?? null, $spec);
            if ($v !== null || !$required) {
                $args[$prop] = $v;
            }
        }
        return new self(...$args + self::defaults());
    }

    /** @return array<string, mixed> */
    public function toArray(): array
    {
        $out = [];
        foreach (self::FIELDS as [$prop, $wire, $spec, $required]) {
            $v = $this->{$prop};
            if (!$required && ($v === null || $v === '')) {
                continue;
            }
            $out[$wire] = Wire::toWire($v, $spec);
        }
        return $out;
    }

    /** @return array<string, mixed> Zero values for required fields a server left out. */
    private static function defaults(): array
    {
        return [
            'entries' => 0,
            'ownedProjects' => [],
            'tokens' => 0,
            'userId' => '',
        ];
    }
}
