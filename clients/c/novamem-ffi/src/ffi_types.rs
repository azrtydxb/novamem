// Code generated by clients/gen. DO NOT EDIT.
//
// The Rust side of the C ABI: a #[repr(C)] mirror of every wire type, in
// the field order clients/c/include/novamem.h declares (both are generated
// from one model), with conversions to and from the Rust SDK's types and a
// novamem_<type>_free for each.

#![allow(non_camel_case_types, clippy::all, unused_unsafe, dead_code)]
// Formatting a generated file would fight the generator; rustfmt alone
// sets the `rustfmt` cfg, so the compiler never sees this attribute.
#![cfg_attr(rustfmt, rustfmt_skip)]

use std::os::raw::c_char;

use novamem::types;

use crate::conv::*;


#[repr(C)]
pub struct novamem_active_project {
    pub active: *mut novamem_active_project_active,
}

impl novamem_active_project {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ActiveProject {
        types::ActiveProject {
            active: self.active.as_ref().map(|p| p.to_rust()),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ActiveProject) -> Self {
        Self {
            active: v.active.as_ref().map_or(std::ptr::null_mut(), |x| boxed(novamem_active_project_active::to_c(x))),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        novamem_active_project_active_free(self.active);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_active_project_free(p: *mut novamem_active_project) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_active_project_active {
    pub id: *mut c_char,
    pub name: *mut c_char,
}

impl novamem_active_project_active {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ActiveProjectActive {
        types::ActiveProjectActive {
            id: c_str(self.id),
            name: c_str(self.name),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ActiveProjectActive) -> Self {
        Self {
            id: str_to_c(v.id.as_deref()),
            name: str_to_c(v.name.as_deref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.id);
        free_str(self.name);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_active_project_active_free(p: *mut novamem_active_project_active) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_admin_user {
    pub created_at: *mut c_char,
    pub email: *mut c_char,
    pub entry_count: i32,
    pub id: *mut c_char,
    pub name: *mut c_char,
    pub role: *mut c_char,
    pub token_count: i32,
}

impl novamem_admin_user {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::AdminUser {
        types::AdminUser {
            created_at: c_str(self.created_at).unwrap_or_default(),
            email: c_str(self.email).unwrap_or_default(),
            entry_count: self.entry_count,
            id: c_str(self.id).unwrap_or_default(),
            name: c_str(self.name),
            role: c_str(self.role).unwrap_or_default(),
            token_count: self.token_count,
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::AdminUser) -> Self {
        Self {
            created_at: str_to_c(Some(v.created_at.as_str())),
            email: str_to_c(Some(v.email.as_str())),
            entry_count: v.entry_count,
            id: str_to_c(Some(v.id.as_str())),
            name: str_to_c(v.name.as_deref()),
            role: str_to_c(Some(v.role.as_str())),
            token_count: v.token_count,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.created_at);
        free_str(self.email);

        free_str(self.id);
        free_str(self.name);
        free_str(self.role);

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_admin_user_free(p: *mut novamem_admin_user) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_admin_user_list {
    pub users: *mut novamem_admin_user,
    pub users_len: usize,
}

impl novamem_admin_user_list {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::AdminUserList {
        types::AdminUserList {
            users: c_obj_array(self.users, self.users_len, |x| x.to_rust()).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::AdminUserList) -> Self {
        let (users_ptr, users_len) = obj_array_to_c(Some(&v.users[..]), novamem_admin_user::to_c);
        Self {
            users: users_ptr,
            users_len,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_obj_array(self.users, self.users_len, |x| x.free_fields());
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_admin_user_list_free(p: *mut novamem_admin_user_list) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_adoption_report {
    pub diagnostics: *mut c_char,
    pub features: *mut c_char,
    pub mcp: *mut c_char,
    pub refresh: *mut c_char,
    pub requested_client: *mut c_char,
    pub required_tools: *mut *mut c_char,
    pub required_tools_len: usize,
    pub server: *mut c_char,
}

impl novamem_adoption_report {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::AdoptionReport {
        types::AdoptionReport {
            diagnostics: c_json(self.diagnostics).unwrap_or_default(),
            features: c_json(self.features),
            mcp: c_json(self.mcp).unwrap_or_default(),
            refresh: c_json(self.refresh),
            requested_client: c_str(self.requested_client).unwrap_or_default(),
            required_tools: c_str_array(self.required_tools, self.required_tools_len),
            server: c_json(self.server).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::AdoptionReport) -> Self {
        let (required_tools_ptr, required_tools_len) = str_array_to_c(v.required_tools.as_deref());
        Self {
            diagnostics: json_to_c(Some(&v.diagnostics)),
            features: json_to_c(v.features.as_ref()),
            mcp: json_to_c(Some(&v.mcp)),
            refresh: json_to_c(v.refresh.as_ref()),
            requested_client: str_to_c(Some(v.requested_client.as_str())),
            required_tools: required_tools_ptr,
            required_tools_len,
            server: json_to_c(Some(&v.server)),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.diagnostics);
        free_str(self.features);
        free_str(self.mcp);
        free_str(self.refresh);
        free_str(self.requested_client);
        free_str_array(self.required_tools, self.required_tools_len);
        free_str(self.server);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_adoption_report_free(p: *mut novamem_adoption_report) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_capture_request {
    pub agent_name: *mut c_char,
    pub captured_from: *mut c_char,
    pub has_confidence: bool,
    pub confidence: f64,
    pub content: *mut c_char,
    pub expires_at: *mut c_char,
    pub has_force: bool,
    pub force: bool,
    pub metadata: *mut c_char,
    pub namespace: *mut c_char,
    pub project: *mut c_char,
    pub sensitivity: *mut c_char,
    pub source: *mut c_char,
    pub source_type: *mut c_char,
}

impl novamem_capture_request {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::CaptureRequest {
        types::CaptureRequest {
            agent_name: c_str(self.agent_name),
            captured_from: c_str(self.captured_from),
            confidence: if self.has_confidence { Some(self.confidence) } else { None },
            content: c_str(self.content).unwrap_or_default(),
            expires_at: c_str(self.expires_at),
            force: if self.has_force { Some(self.force) } else { None },
            metadata: c_json(self.metadata),
            namespace: c_str(self.namespace),
            project: c_str(self.project),
            sensitivity: c_enum(self.sensitivity),
            source: c_str(self.source),
            source_type: c_str(self.source_type),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::CaptureRequest) -> Self {
        Self {
            agent_name: str_to_c(v.agent_name.as_deref()),
            captured_from: str_to_c(v.captured_from.as_deref()),
            has_confidence: v.confidence.is_some(),
            confidence: v.confidence.unwrap_or_default(),
            content: str_to_c(Some(v.content.as_str())),
            expires_at: str_to_c(v.expires_at.as_deref()),
            has_force: v.force.is_some(),
            force: v.force.unwrap_or_default(),
            metadata: json_to_c(v.metadata.as_ref()),
            namespace: str_to_c(v.namespace.as_deref()),
            project: str_to_c(v.project.as_deref()),
            sensitivity: enum_to_c(v.sensitivity.as_ref()),
            source: str_to_c(v.source.as_deref()),
            source_type: str_to_c(v.source_type.as_deref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.agent_name);
        free_str(self.captured_from);

        free_str(self.content);
        free_str(self.expires_at);

        free_str(self.metadata);
        free_str(self.namespace);
        free_str(self.project);
        free_str(self.sensitivity);
        free_str(self.source);
        free_str(self.source_type);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_capture_request_free(p: *mut novamem_capture_request) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_capture_result {
    pub has_deduplicated: bool,
    pub deduplicated: bool,
    pub has_embedded: bool,
    pub embedded: bool,
    pub id: *mut c_char,
    pub rejected: *mut c_char,
    pub superseded: *mut *mut c_char,
    pub superseded_len: usize,
    pub has_updated: bool,
    pub updated: bool,
}

impl novamem_capture_result {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::CaptureResult {
        types::CaptureResult {
            deduplicated: if self.has_deduplicated { Some(self.deduplicated) } else { None },
            embedded: if self.has_embedded { Some(self.embedded) } else { None },
            id: c_str(self.id),
            rejected: c_str(self.rejected),
            superseded: c_str_array(self.superseded, self.superseded_len),
            updated: if self.has_updated { Some(self.updated) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::CaptureResult) -> Self {
        let (superseded_ptr, superseded_len) = str_array_to_c(v.superseded.as_deref());
        Self {
            has_deduplicated: v.deduplicated.is_some(),
            deduplicated: v.deduplicated.unwrap_or_default(),
            has_embedded: v.embedded.is_some(),
            embedded: v.embedded.unwrap_or_default(),
            id: str_to_c(v.id.as_deref()),
            rejected: str_to_c(v.rejected.as_deref()),
            superseded: superseded_ptr,
            superseded_len,
            has_updated: v.updated.is_some(),
            updated: v.updated.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {


        free_str(self.id);
        free_str(self.rejected);
        free_str_array(self.superseded, self.superseded_len);

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_capture_result_free(p: *mut novamem_capture_result) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_change_feed {
    pub changes: *mut novamem_change_feed_changes_item,
    pub changes_len: usize,
    pub has_next_seq: bool,
    pub next_seq: i64,
}

impl novamem_change_feed {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ChangeFeed {
        types::ChangeFeed {
            changes: c_obj_array(self.changes, self.changes_len, |x| x.to_rust()).unwrap_or_default(),
            next_seq: if self.has_next_seq { Some(self.next_seq) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ChangeFeed) -> Self {
        let (changes_ptr, changes_len) = obj_array_to_c(Some(&v.changes[..]), novamem_change_feed_changes_item::to_c);
        Self {
            changes: changes_ptr,
            changes_len,
            has_next_seq: v.next_seq.is_some(),
            next_seq: v.next_seq.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_obj_array(self.changes, self.changes_len, |x| x.free_fields());

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_change_feed_free(p: *mut novamem_change_feed) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_change_feed_changes_item {
    pub at: *mut c_char,
    pub change: *mut c_char,
    pub detail: *mut c_char,
    pub entry_id: *mut c_char,
    pub project_id: *mut c_char,
    pub has_seq: bool,
    pub seq: i64,
}

impl novamem_change_feed_changes_item {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ChangeFeedChangesItem {
        types::ChangeFeedChangesItem {
            at: c_str(self.at),
            change: c_enum(self.change),
            detail: c_json(self.detail),
            entry_id: c_str(self.entry_id),
            project_id: c_str(self.project_id),
            seq: if self.has_seq { Some(self.seq) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ChangeFeedChangesItem) -> Self {
        Self {
            at: str_to_c(v.at.as_deref()),
            change: enum_to_c(v.change.as_ref()),
            detail: json_to_c(v.detail.as_ref()),
            entry_id: str_to_c(v.entry_id.as_deref()),
            project_id: str_to_c(v.project_id.as_deref()),
            has_seq: v.seq.is_some(),
            seq: v.seq.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.at);
        free_str(self.change);
        free_str(self.detail);
        free_str(self.entry_id);
        free_str(self.project_id);

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_change_feed_changes_item_free(p: *mut novamem_change_feed_changes_item) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_context_prefix {
    pub prefix: *mut c_char,
}

impl novamem_context_prefix {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ContextPrefix {
        types::ContextPrefix {
            prefix: c_str(self.prefix).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ContextPrefix) -> Self {
        Self {
            prefix: str_to_c(Some(v.prefix.as_str())),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.prefix);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_context_prefix_free(p: *mut novamem_context_prefix) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_context_request {
    pub as_of: *mut c_char,
    pub has_decompose: bool,
    pub decompose: bool,
    pub has_expand_source_chunks: bool,
    pub expand_source_chunks: bool,
    pub include_namespaces: *mut *mut c_char,
    pub include_namespaces_len: usize,
    pub include_projects: *mut *mut c_char,
    pub include_projects_len: usize,
    pub has_k: bool,
    pub k: i32,
    pub max_sensitivity: *mut c_char,
    pub has_max_tokens: bool,
    pub max_tokens: i32,
    pub message: *mut c_char,
    pub namespace: *mut c_char,
    pub project: *mut c_char,
    pub weights: *mut novamem_context_request_weights,
}

impl novamem_context_request {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ContextRequest {
        types::ContextRequest {
            as_of: c_str(self.as_of),
            decompose: if self.has_decompose { Some(self.decompose) } else { None },
            expand_source_chunks: if self.has_expand_source_chunks { Some(self.expand_source_chunks) } else { None },
            include_namespaces: c_str_array(self.include_namespaces, self.include_namespaces_len),
            include_projects: c_str_array(self.include_projects, self.include_projects_len),
            k: if self.has_k { Some(self.k) } else { None },
            max_sensitivity: c_enum(self.max_sensitivity),
            max_tokens: if self.has_max_tokens { Some(self.max_tokens) } else { None },
            message: c_str(self.message).unwrap_or_default(),
            namespace: c_str(self.namespace),
            project: c_str(self.project),
            weights: self.weights.as_ref().map(|p| p.to_rust()),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ContextRequest) -> Self {
        let (include_namespaces_ptr, include_namespaces_len) = str_array_to_c(v.include_namespaces.as_deref());
        let (include_projects_ptr, include_projects_len) = str_array_to_c(v.include_projects.as_deref());
        Self {
            as_of: str_to_c(v.as_of.as_deref()),
            has_decompose: v.decompose.is_some(),
            decompose: v.decompose.unwrap_or_default(),
            has_expand_source_chunks: v.expand_source_chunks.is_some(),
            expand_source_chunks: v.expand_source_chunks.unwrap_or_default(),
            include_namespaces: include_namespaces_ptr,
            include_namespaces_len,
            include_projects: include_projects_ptr,
            include_projects_len,
            has_k: v.k.is_some(),
            k: v.k.unwrap_or_default(),
            max_sensitivity: enum_to_c(v.max_sensitivity.as_ref()),
            has_max_tokens: v.max_tokens.is_some(),
            max_tokens: v.max_tokens.unwrap_or_default(),
            message: str_to_c(Some(v.message.as_str())),
            namespace: str_to_c(v.namespace.as_deref()),
            project: str_to_c(v.project.as_deref()),
            weights: v.weights.as_ref().map_or(std::ptr::null_mut(), |x| boxed(novamem_context_request_weights::to_c(x))),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.as_of);


        free_str_array(self.include_namespaces, self.include_namespaces_len);
        free_str_array(self.include_projects, self.include_projects_len);

        free_str(self.max_sensitivity);

        free_str(self.message);
        free_str(self.namespace);
        free_str(self.project);
        novamem_context_request_weights_free(self.weights);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_context_request_free(p: *mut novamem_context_request) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_context_request_weights {
    pub has_entity: bool,
    pub entity: f64,
    pub has_graph: bool,
    pub graph: f64,
    pub has_keyword: bool,
    pub keyword: f64,
    pub has_recency: bool,
    pub recency: f64,
    pub has_vector: bool,
    pub vector: f64,
}

impl novamem_context_request_weights {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ContextRequestWeights {
        types::ContextRequestWeights {
            entity: if self.has_entity { Some(self.entity) } else { None },
            graph: if self.has_graph { Some(self.graph) } else { None },
            keyword: if self.has_keyword { Some(self.keyword) } else { None },
            recency: if self.has_recency { Some(self.recency) } else { None },
            vector: if self.has_vector { Some(self.vector) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ContextRequestWeights) -> Self {
        Self {
            has_entity: v.entity.is_some(),
            entity: v.entity.unwrap_or_default(),
            has_graph: v.graph.is_some(),
            graph: v.graph.unwrap_or_default(),
            has_keyword: v.keyword.is_some(),
            keyword: v.keyword.unwrap_or_default(),
            has_recency: v.recency.is_some(),
            recency: v.recency.unwrap_or_default(),
            has_vector: v.vector.is_some(),
            vector: v.vector.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {





    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_context_request_weights_free(p: *mut novamem_context_request_weights) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_decay_result {
    pub demoted: f64,
    pub expired: f64,
    pub promoted: f64,
}

impl novamem_decay_result {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::DecayResult {
        types::DecayResult {
            demoted: self.demoted,
            expired: self.expired,
            promoted: self.promoted,
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::DecayResult) -> Self {
        Self {
            demoted: v.demoted,
            expired: v.expired,
            promoted: v.promoted,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {



    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_decay_result_free(p: *mut novamem_decay_result) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_entry_list {
    pub results: *mut novamem_memory_entry,
    pub results_len: usize,
}

impl novamem_entry_list {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::EntryList {
        types::EntryList {
            results: c_obj_array(self.results, self.results_len, |x| x.to_rust()).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::EntryList) -> Self {
        let (results_ptr, results_len) = obj_array_to_c(Some(&v.results[..]), novamem_memory_entry::to_c);
        Self {
            results: results_ptr,
            results_len,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_obj_array(self.results, self.results_len, |x| x.free_fields());
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_entry_list_free(p: *mut novamem_entry_list) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_evaluate_report {
    pub cases: *mut novamem_evaluate_report_cases_item,
    pub cases_len: usize,
    pub passed: bool,
    pub suite: *mut c_char,
    pub summary: *mut novamem_evaluate_report_summary,
}

impl novamem_evaluate_report {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::EvaluateReport {
        types::EvaluateReport {
            cases: c_obj_array(self.cases, self.cases_len, |x| x.to_rust()).unwrap_or_default(),
            passed: self.passed,
            suite: c_str(self.suite).unwrap_or_default(),
            summary: self.summary.as_ref().map(|p| p.to_rust()).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::EvaluateReport) -> Self {
        let (cases_ptr, cases_len) = obj_array_to_c(Some(&v.cases[..]), novamem_evaluate_report_cases_item::to_c);
        Self {
            cases: cases_ptr,
            cases_len,
            passed: v.passed,
            suite: str_to_c(Some(v.suite.as_str())),
            summary: boxed(novamem_evaluate_report_summary::to_c(&v.summary)),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_obj_array(self.cases, self.cases_len, |x| x.free_fields());

        free_str(self.suite);
        novamem_evaluate_report_summary_free(self.summary);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_evaluate_report_free(p: *mut novamem_evaluate_report) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_evaluate_report_cases_item {
    pub name: *mut c_char,
    pub has_passed: bool,
    pub passed: bool,
}

impl novamem_evaluate_report_cases_item {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::EvaluateReportCasesItem {
        types::EvaluateReportCasesItem {
            name: c_str(self.name),
            passed: if self.has_passed { Some(self.passed) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::EvaluateReportCasesItem) -> Self {
        Self {
            name: str_to_c(v.name.as_deref()),
            has_passed: v.passed.is_some(),
            passed: v.passed.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.name);

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_evaluate_report_cases_item_free(p: *mut novamem_evaluate_report_cases_item) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_evaluate_report_summary {
    pub has_failed: bool,
    pub failed: f64,
    pub has_passed: bool,
    pub passed: f64,
    pub has_total: bool,
    pub total: f64,
}

impl novamem_evaluate_report_summary {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::EvaluateReportSummary {
        types::EvaluateReportSummary {
            failed: if self.has_failed { Some(self.failed) } else { None },
            passed: if self.has_passed { Some(self.passed) } else { None },
            total: if self.has_total { Some(self.total) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::EvaluateReportSummary) -> Self {
        Self {
            has_failed: v.failed.is_some(),
            failed: v.failed.unwrap_or_default(),
            has_passed: v.passed.is_some(),
            passed: v.passed.unwrap_or_default(),
            has_total: v.total.is_some(),
            total: v.total.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {



    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_evaluate_report_summary_free(p: *mut novamem_evaluate_report_summary) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_export_page {
    pub entries: *mut novamem_exported_entry,
    pub entries_len: usize,
    pub next_after_id: *mut c_char,
}

impl novamem_export_page {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ExportPage {
        types::ExportPage {
            entries: c_obj_array(self.entries, self.entries_len, |x| x.to_rust()).unwrap_or_default(),
            next_after_id: c_str(self.next_after_id),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ExportPage) -> Self {
        let (entries_ptr, entries_len) = obj_array_to_c(Some(&v.entries[..]), novamem_exported_entry::to_c);
        Self {
            entries: entries_ptr,
            entries_len,
            next_after_id: str_to_c(v.next_after_id.as_deref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_obj_array(self.entries, self.entries_len, |x| x.free_fields());
        free_str(self.next_after_id);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_export_page_free(p: *mut novamem_export_page) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_exported_entry {
    pub agent_name: *mut c_char,
    pub captured_from: *mut c_char,
    pub confidence: f64,
    pub content: *mut c_char,
    pub created_at: *mut c_char,
    pub id: *mut c_char,
    pub metadata: *mut c_char,
    pub namespace: *mut c_char,
    pub project_id: *mut c_char,
    pub source: *mut c_char,
    pub source_type: *mut c_char,
    pub updated_at: *mut c_char,
}

impl novamem_exported_entry {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ExportedEntry {
        types::ExportedEntry {
            agent_name: c_str(self.agent_name),
            captured_from: c_str(self.captured_from),
            confidence: self.confidence,
            content: c_str(self.content).unwrap_or_default(),
            created_at: c_str(self.created_at).unwrap_or_default(),
            id: c_str(self.id).unwrap_or_default(),
            metadata: c_json(self.metadata).unwrap_or_default(),
            namespace: c_str(self.namespace).unwrap_or_default(),
            project_id: c_str(self.project_id),
            source: c_str(self.source).unwrap_or_default(),
            source_type: c_str(self.source_type),
            updated_at: c_str(self.updated_at).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ExportedEntry) -> Self {
        Self {
            agent_name: str_to_c(v.agent_name.as_deref()),
            captured_from: str_to_c(v.captured_from.as_deref()),
            confidence: v.confidence,
            content: str_to_c(Some(v.content.as_str())),
            created_at: str_to_c(Some(v.created_at.as_str())),
            id: str_to_c(Some(v.id.as_str())),
            metadata: json_to_c(Some(&v.metadata)),
            namespace: str_to_c(Some(v.namespace.as_str())),
            project_id: str_to_c(v.project_id.as_deref()),
            source: str_to_c(Some(v.source.as_str())),
            source_type: str_to_c(v.source_type.as_deref()),
            updated_at: str_to_c(Some(v.updated_at.as_str())),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.agent_name);
        free_str(self.captured_from);

        free_str(self.content);
        free_str(self.created_at);
        free_str(self.id);
        free_str(self.metadata);
        free_str(self.namespace);
        free_str(self.project_id);
        free_str(self.source);
        free_str(self.source_type);
        free_str(self.updated_at);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_exported_entry_free(p: *mut novamem_exported_entry) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_forget_request {
    pub id: *mut c_char,
    pub project: *mut c_char,
}

impl novamem_forget_request {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ForgetRequest {
        types::ForgetRequest {
            id: c_str(self.id).unwrap_or_default(),
            project: c_str(self.project),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ForgetRequest) -> Self {
        Self {
            id: str_to_c(Some(v.id.as_str())),
            project: str_to_c(v.project.as_deref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.id);
        free_str(self.project);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_forget_request_free(p: *mut novamem_forget_request) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_forget_result {
    pub has_cold_delete_ok: bool,
    pub cold_delete_ok: bool,
    pub deleted: bool,
}

impl novamem_forget_result {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ForgetResult {
        types::ForgetResult {
            cold_delete_ok: if self.has_cold_delete_ok { Some(self.cold_delete_ok) } else { None },
            deleted: self.deleted,
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ForgetResult) -> Self {
        Self {
            has_cold_delete_ok: v.cold_delete_ok.is_some(),
            cold_delete_ok: v.cold_delete_ok.unwrap_or_default(),
            deleted: v.deleted,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {


    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_forget_result_free(p: *mut novamem_forget_result) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_health {
    pub ok: bool,
}

impl novamem_health {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::Health {
        types::Health {
            ok: self.ok,
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::Health) -> Self {
        Self {
            ok: v.ok,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_health_free(p: *mut novamem_health) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_hygiene_report {
    pub contradiction_candidates: *mut c_char,
    pub duplicate_clusters: *mut c_char,
    pub low_value: *mut c_char,
    pub orphan_candidates: *mut c_char,
    pub stale: *mut c_char,
    pub summary: *mut novamem_hygiene_report_summary,
}

impl novamem_hygiene_report {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::HygieneReport {
        types::HygieneReport {
            contradiction_candidates: c_json(self.contradiction_candidates),
            duplicate_clusters: c_json(self.duplicate_clusters),
            low_value: c_json(self.low_value),
            orphan_candidates: c_json(self.orphan_candidates),
            stale: c_json(self.stale),
            summary: self.summary.as_ref().map(|p| p.to_rust()).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::HygieneReport) -> Self {
        Self {
            contradiction_candidates: json_to_c(v.contradiction_candidates.as_ref()),
            duplicate_clusters: json_to_c(v.duplicate_clusters.as_ref()),
            low_value: json_to_c(v.low_value.as_ref()),
            orphan_candidates: json_to_c(v.orphan_candidates.as_ref()),
            stale: json_to_c(v.stale.as_ref()),
            summary: boxed(novamem_hygiene_report_summary::to_c(&v.summary)),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.contradiction_candidates);
        free_str(self.duplicate_clusters);
        free_str(self.low_value);
        free_str(self.orphan_candidates);
        free_str(self.stale);
        novamem_hygiene_report_summary_free(self.summary);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_hygiene_report_free(p: *mut novamem_hygiene_report) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_hygiene_report_summary {
    pub has_contradiction_candidates: bool,
    pub contradiction_candidates: f64,
    pub has_duplicate_clusters: bool,
    pub duplicate_clusters: f64,
    pub has_low_value: bool,
    pub low_value: f64,
    pub has_orphan_candidates: bool,
    pub orphan_candidates: f64,
    pub has_scanned: bool,
    pub scanned: f64,
    pub has_stale: bool,
    pub stale: f64,
}

impl novamem_hygiene_report_summary {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::HygieneReportSummary {
        types::HygieneReportSummary {
            contradiction_candidates: if self.has_contradiction_candidates { Some(self.contradiction_candidates) } else { None },
            duplicate_clusters: if self.has_duplicate_clusters { Some(self.duplicate_clusters) } else { None },
            low_value: if self.has_low_value { Some(self.low_value) } else { None },
            orphan_candidates: if self.has_orphan_candidates { Some(self.orphan_candidates) } else { None },
            scanned: if self.has_scanned { Some(self.scanned) } else { None },
            stale: if self.has_stale { Some(self.stale) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::HygieneReportSummary) -> Self {
        Self {
            has_contradiction_candidates: v.contradiction_candidates.is_some(),
            contradiction_candidates: v.contradiction_candidates.unwrap_or_default(),
            has_duplicate_clusters: v.duplicate_clusters.is_some(),
            duplicate_clusters: v.duplicate_clusters.unwrap_or_default(),
            has_low_value: v.low_value.is_some(),
            low_value: v.low_value.unwrap_or_default(),
            has_orphan_candidates: v.orphan_candidates.is_some(),
            orphan_candidates: v.orphan_candidates.unwrap_or_default(),
            has_scanned: v.scanned.is_some(),
            scanned: v.scanned.unwrap_or_default(),
            has_stale: v.stale.is_some(),
            stale: v.stale.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {






    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_hygiene_report_summary_free(p: *mut novamem_hygiene_report_summary) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_import_result {
    pub deduplicated: i32,
    pub failed: *mut novamem_import_result_failed_item,
    pub failed_len: usize,
    pub imported: i32,
}

impl novamem_import_result {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ImportResult {
        types::ImportResult {
            deduplicated: self.deduplicated,
            failed: c_obj_array(self.failed, self.failed_len, |x| x.to_rust()).unwrap_or_default(),
            imported: self.imported,
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ImportResult) -> Self {
        let (failed_ptr, failed_len) = obj_array_to_c(Some(&v.failed[..]), novamem_import_result_failed_item::to_c);
        Self {
            deduplicated: v.deduplicated,
            failed: failed_ptr,
            failed_len,
            imported: v.imported,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {

        free_obj_array(self.failed, self.failed_len, |x| x.free_fields());

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_import_result_free(p: *mut novamem_import_result) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_import_result_failed_item {
    pub error: *mut c_char,
    pub index: i32,
}

impl novamem_import_result_failed_item {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ImportResultFailedItem {
        types::ImportResultFailedItem {
            error: c_str(self.error).unwrap_or_default(),
            index: self.index,
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ImportResultFailedItem) -> Self {
        Self {
            error: str_to_c(Some(v.error.as_str())),
            index: v.index,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.error);

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_import_result_failed_item_free(p: *mut novamem_import_result_failed_item) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_member_added {
    pub added: bool,
    pub user_id: *mut c_char,
    pub username: *mut c_char,
}

impl novamem_member_added {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::MemberAdded {
        types::MemberAdded {
            added: self.added,
            user_id: c_str(self.user_id).unwrap_or_default(),
            username: c_str(self.username).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::MemberAdded) -> Self {
        Self {
            added: v.added,
            user_id: str_to_c(Some(v.user_id.as_str())),
            username: str_to_c(Some(v.username.as_str())),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {

        free_str(self.user_id);
        free_str(self.username);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_member_added_free(p: *mut novamem_member_added) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_member_list {
    pub members: *mut novamem_member_list_members_item,
    pub members_len: usize,
}

impl novamem_member_list {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::MemberList {
        types::MemberList {
            members: c_obj_array(self.members, self.members_len, |x| x.to_rust()).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::MemberList) -> Self {
        let (members_ptr, members_len) = obj_array_to_c(Some(&v.members[..]), novamem_member_list_members_item::to_c);
        Self {
            members: members_ptr,
            members_len,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_obj_array(self.members, self.members_len, |x| x.free_fields());
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_member_list_free(p: *mut novamem_member_list) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_member_list_members_item {
    pub joined_at: *mut c_char,
    pub role: *mut c_char,
    pub user_id: *mut c_char,
    pub username: *mut c_char,
}

impl novamem_member_list_members_item {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::MemberListMembersItem {
        types::MemberListMembersItem {
            joined_at: c_str(self.joined_at),
            role: c_str(self.role),
            user_id: c_str(self.user_id),
            username: c_str(self.username),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::MemberListMembersItem) -> Self {
        Self {
            joined_at: str_to_c(v.joined_at.as_deref()),
            role: str_to_c(v.role.as_deref()),
            user_id: str_to_c(v.user_id.as_deref()),
            username: str_to_c(v.username.as_deref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.joined_at);
        free_str(self.role);
        free_str(self.user_id);
        free_str(self.username);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_member_list_members_item_free(p: *mut novamem_member_list_members_item) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_member_removed {
    pub removed: bool,
}

impl novamem_member_removed {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::MemberRemoved {
        types::MemberRemoved {
            removed: self.removed,
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::MemberRemoved) -> Self {
        Self {
            removed: v.removed,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_member_removed_free(p: *mut novamem_member_removed) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_memory_entry {
    pub content: *mut c_char,
    pub id: *mut c_char,
    pub metadata: *mut c_char,
    pub namespace: *mut c_char,
    pub project: *mut c_char,
    pub has_score: bool,
    pub score: f64,
    pub signals: *mut novamem_memory_entry_signals,
    pub source: *mut c_char,
    pub tier: *mut c_char,
}

impl novamem_memory_entry {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::MemoryEntry {
        types::MemoryEntry {
            content: c_str(self.content).unwrap_or_default(),
            id: c_str(self.id).unwrap_or_default(),
            metadata: c_json(self.metadata),
            namespace: c_str(self.namespace),
            project: c_str(self.project),
            score: if self.has_score { Some(self.score) } else { None },
            signals: self.signals.as_ref().map(|p| p.to_rust()),
            source: c_str(self.source),
            tier: c_enum(self.tier),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::MemoryEntry) -> Self {
        Self {
            content: str_to_c(Some(v.content.as_str())),
            id: str_to_c(Some(v.id.as_str())),
            metadata: json_to_c(v.metadata.as_ref()),
            namespace: str_to_c(v.namespace.as_deref()),
            project: str_to_c(v.project.as_deref()),
            has_score: v.score.is_some(),
            score: v.score.unwrap_or_default(),
            signals: v.signals.as_ref().map_or(std::ptr::null_mut(), |x| boxed(novamem_memory_entry_signals::to_c(x))),
            source: str_to_c(v.source.as_deref()),
            tier: enum_to_c(v.tier.as_ref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.content);
        free_str(self.id);
        free_str(self.metadata);
        free_str(self.namespace);
        free_str(self.project);

        novamem_memory_entry_signals_free(self.signals);
        free_str(self.source);
        free_str(self.tier);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_memory_entry_free(p: *mut novamem_memory_entry) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_memory_entry_signals {
    pub has_entity: bool,
    pub entity: f64,
    pub has_graph: bool,
    pub graph: f64,
    pub has_keyword: bool,
    pub keyword: f64,
    pub has_recency: bool,
    pub recency: f64,
    pub has_vector: bool,
    pub vector: f64,
}

impl novamem_memory_entry_signals {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::MemoryEntrySignals {
        types::MemoryEntrySignals {
            entity: if self.has_entity { Some(self.entity) } else { None },
            graph: if self.has_graph { Some(self.graph) } else { None },
            keyword: if self.has_keyword { Some(self.keyword) } else { None },
            recency: if self.has_recency { Some(self.recency) } else { None },
            vector: if self.has_vector { Some(self.vector) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::MemoryEntrySignals) -> Self {
        Self {
            has_entity: v.entity.is_some(),
            entity: v.entity.unwrap_or_default(),
            has_graph: v.graph.is_some(),
            graph: v.graph.unwrap_or_default(),
            has_keyword: v.keyword.is_some(),
            keyword: v.keyword.unwrap_or_default(),
            has_recency: v.recency.is_some(),
            recency: v.recency.unwrap_or_default(),
            has_vector: v.vector.is_some(),
            vector: v.vector.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {





    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_memory_entry_signals_free(p: *mut novamem_memory_entry_signals) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_mint_token_request {
    pub has_expires_in_days: bool,
    pub expires_in_days: i32,
    pub label: *mut c_char,
    pub project: *mut c_char,
    pub scope: *mut c_char,
}

impl novamem_mint_token_request {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::MintTokenRequest {
        types::MintTokenRequest {
            expires_in_days: if self.has_expires_in_days { Some(self.expires_in_days) } else { None },
            label: c_str(self.label),
            project: c_str(self.project),
            scope: c_enum(self.scope),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::MintTokenRequest) -> Self {
        Self {
            has_expires_in_days: v.expires_in_days.is_some(),
            expires_in_days: v.expires_in_days.unwrap_or_default(),
            label: str_to_c(v.label.as_deref()),
            project: str_to_c(v.project.as_deref()),
            scope: enum_to_c(v.scope.as_ref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {

        free_str(self.label);
        free_str(self.project);
        free_str(self.scope);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_mint_token_request_free(p: *mut novamem_mint_token_request) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_minted_token {
    pub created_at: *mut c_char,
    pub expires_at: *mut c_char,
    pub project_id: *mut c_char,
    pub scope: *mut c_char,
    pub token: *mut c_char,
    pub user_id: *mut c_char,
    pub warning: *mut c_char,
}

impl novamem_minted_token {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::MintedToken {
        types::MintedToken {
            created_at: c_str(self.created_at),
            expires_at: c_str(self.expires_at),
            project_id: c_str(self.project_id),
            scope: c_str(self.scope),
            token: c_str(self.token).unwrap_or_default(),
            user_id: c_str(self.user_id),
            warning: c_str(self.warning),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::MintedToken) -> Self {
        Self {
            created_at: str_to_c(v.created_at.as_deref()),
            expires_at: str_to_c(v.expires_at.as_deref()),
            project_id: str_to_c(v.project_id.as_deref()),
            scope: str_to_c(v.scope.as_deref()),
            token: str_to_c(Some(v.token.as_str())),
            user_id: str_to_c(v.user_id.as_deref()),
            warning: str_to_c(v.warning.as_deref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.created_at);
        free_str(self.expires_at);
        free_str(self.project_id);
        free_str(self.scope);
        free_str(self.token);
        free_str(self.user_id);
        free_str(self.warning);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_minted_token_free(p: *mut novamem_minted_token) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_neighbors_request {
    pub has_depth: bool,
    pub depth: i32,
    pub id: *mut c_char,
    pub include_namespaces: *mut *mut c_char,
    pub include_namespaces_len: usize,
    pub include_projects: *mut *mut c_char,
    pub include_projects_len: usize,
    pub has_k: bool,
    pub k: i32,
    pub max_sensitivity: *mut c_char,
    pub project: *mut c_char,
}

impl novamem_neighbors_request {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::NeighborsRequest {
        types::NeighborsRequest {
            depth: if self.has_depth { Some(self.depth) } else { None },
            id: c_str(self.id).unwrap_or_default(),
            include_namespaces: c_str_array(self.include_namespaces, self.include_namespaces_len),
            include_projects: c_str_array(self.include_projects, self.include_projects_len),
            k: if self.has_k { Some(self.k) } else { None },
            max_sensitivity: c_enum(self.max_sensitivity),
            project: c_str(self.project),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::NeighborsRequest) -> Self {
        let (include_namespaces_ptr, include_namespaces_len) = str_array_to_c(v.include_namespaces.as_deref());
        let (include_projects_ptr, include_projects_len) = str_array_to_c(v.include_projects.as_deref());
        Self {
            has_depth: v.depth.is_some(),
            depth: v.depth.unwrap_or_default(),
            id: str_to_c(Some(v.id.as_str())),
            include_namespaces: include_namespaces_ptr,
            include_namespaces_len,
            include_projects: include_projects_ptr,
            include_projects_len,
            has_k: v.k.is_some(),
            k: v.k.unwrap_or_default(),
            max_sensitivity: enum_to_c(v.max_sensitivity.as_ref()),
            project: str_to_c(v.project.as_deref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {

        free_str(self.id);
        free_str_array(self.include_namespaces, self.include_namespaces_len);
        free_str_array(self.include_projects, self.include_projects_len);

        free_str(self.max_sensitivity);
        free_str(self.project);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_neighbors_request_free(p: *mut novamem_neighbors_request) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_observe_result {
    pub has_log_chars: bool,
    pub log_chars: f64,
    pub has_observed: bool,
    pub observed: f64,
    pub has_reflected: bool,
    pub reflected: bool,
}

impl novamem_observe_result {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ObserveResult {
        types::ObserveResult {
            log_chars: if self.has_log_chars { Some(self.log_chars) } else { None },
            observed: if self.has_observed { Some(self.observed) } else { None },
            reflected: if self.has_reflected { Some(self.reflected) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ObserveResult) -> Self {
        Self {
            has_log_chars: v.log_chars.is_some(),
            log_chars: v.log_chars.unwrap_or_default(),
            has_observed: v.observed.is_some(),
            observed: v.observed.unwrap_or_default(),
            has_reflected: v.reflected.is_some(),
            reflected: v.reflected.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {



    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_observe_result_free(p: *mut novamem_observe_result) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_project {
    pub created_at: *mut c_char,
    pub id: *mut c_char,
    pub name: *mut c_char,
    pub owner_user_id: *mut c_char,
}

impl novamem_project {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::Project {
        types::Project {
            created_at: c_str(self.created_at),
            id: c_str(self.id).unwrap_or_default(),
            name: c_str(self.name).unwrap_or_default(),
            owner_user_id: c_str(self.owner_user_id),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::Project) -> Self {
        Self {
            created_at: str_to_c(v.created_at.as_deref()),
            id: str_to_c(Some(v.id.as_str())),
            name: str_to_c(Some(v.name.as_str())),
            owner_user_id: str_to_c(v.owner_user_id.as_deref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.created_at);
        free_str(self.id);
        free_str(self.name);
        free_str(self.owner_user_id);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_project_free(p: *mut novamem_project) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_project_deleted {
    pub cold_collections_dropped: *mut *mut c_char,
    pub cold_collections_dropped_len: usize,
    pub deleted: bool,
    pub has_entries_removed: bool,
    pub entries_removed: f64,
    pub has_graph_cleared: bool,
    pub graph_cleared: bool,
}

impl novamem_project_deleted {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ProjectDeleted {
        types::ProjectDeleted {
            cold_collections_dropped: c_str_array(self.cold_collections_dropped, self.cold_collections_dropped_len),
            deleted: self.deleted,
            entries_removed: if self.has_entries_removed { Some(self.entries_removed) } else { None },
            graph_cleared: if self.has_graph_cleared { Some(self.graph_cleared) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ProjectDeleted) -> Self {
        let (cold_collections_dropped_ptr, cold_collections_dropped_len) = str_array_to_c(v.cold_collections_dropped.as_deref());
        Self {
            cold_collections_dropped: cold_collections_dropped_ptr,
            cold_collections_dropped_len,
            deleted: v.deleted,
            has_entries_removed: v.entries_removed.is_some(),
            entries_removed: v.entries_removed.unwrap_or_default(),
            has_graph_cleared: v.graph_cleared.is_some(),
            graph_cleared: v.graph_cleared.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str_array(self.cold_collections_dropped, self.cold_collections_dropped_len);



    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_project_deleted_free(p: *mut novamem_project_deleted) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_project_list {
    pub projects: *mut novamem_project_list_item,
    pub projects_len: usize,
}

impl novamem_project_list {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ProjectList {
        types::ProjectList {
            projects: c_obj_array(self.projects, self.projects_len, |x| x.to_rust()).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ProjectList) -> Self {
        let (projects_ptr, projects_len) = obj_array_to_c(Some(&v.projects[..]), novamem_project_list_item::to_c);
        Self {
            projects: projects_ptr,
            projects_len,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_obj_array(self.projects, self.projects_len, |x| x.free_fields());
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_project_list_free(p: *mut novamem_project_list) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_project_list_item {
    pub created_at: *mut c_char,
    pub id: *mut c_char,
    pub name: *mut c_char,
    pub owner_user_id: *mut c_char,
    pub role: *mut c_char,
}

impl novamem_project_list_item {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ProjectListItem {
        types::ProjectListItem {
            created_at: c_str(self.created_at),
            id: c_str(self.id).unwrap_or_default(),
            name: c_str(self.name).unwrap_or_default(),
            owner_user_id: c_str(self.owner_user_id),
            role: c_str(self.role).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ProjectListItem) -> Self {
        Self {
            created_at: str_to_c(v.created_at.as_deref()),
            id: str_to_c(Some(v.id.as_str())),
            name: str_to_c(Some(v.name.as_str())),
            owner_user_id: str_to_c(v.owner_user_id.as_deref()),
            role: str_to_c(Some(v.role.as_str())),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.created_at);
        free_str(self.id);
        free_str(self.name);
        free_str(self.owner_user_id);
        free_str(self.role);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_project_list_item_free(p: *mut novamem_project_list_item) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_provision_user_request {
    pub email: *mut c_char,
    pub name: *mut c_char,
    pub password: *mut c_char,
    pub has_token_expires_in_days: bool,
    pub token_expires_in_days: i32,
    pub token_label: *mut c_char,
    pub token_scope: *mut c_char,
}

impl novamem_provision_user_request {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ProvisionUserRequest {
        types::ProvisionUserRequest {
            email: c_str(self.email).unwrap_or_default(),
            name: c_str(self.name),
            password: c_str(self.password).unwrap_or_default(),
            token_expires_in_days: if self.has_token_expires_in_days { Some(self.token_expires_in_days) } else { None },
            token_label: c_str(self.token_label),
            token_scope: c_enum(self.token_scope),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ProvisionUserRequest) -> Self {
        Self {
            email: str_to_c(Some(v.email.as_str())),
            name: str_to_c(v.name.as_deref()),
            password: str_to_c(Some(v.password.as_str())),
            has_token_expires_in_days: v.token_expires_in_days.is_some(),
            token_expires_in_days: v.token_expires_in_days.unwrap_or_default(),
            token_label: str_to_c(v.token_label.as_deref()),
            token_scope: enum_to_c(v.token_scope.as_ref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.email);
        free_str(self.name);
        free_str(self.password);

        free_str(self.token_label);
        free_str(self.token_scope);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_provision_user_request_free(p: *mut novamem_provision_user_request) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_provisioned_user {
    pub email: *mut c_char,
    pub token: *mut c_char,
    pub user_id: *mut c_char,
}

impl novamem_provisioned_user {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::ProvisionedUser {
        types::ProvisionedUser {
            email: c_str(self.email).unwrap_or_default(),
            token: c_str(self.token),
            user_id: c_str(self.user_id).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::ProvisionedUser) -> Self {
        Self {
            email: str_to_c(Some(v.email.as_str())),
            token: str_to_c(v.token.as_deref()),
            user_id: str_to_c(Some(v.user_id.as_str())),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.email);
        free_str(self.token);
        free_str(self.user_id);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_provisioned_user_free(p: *mut novamem_provisioned_user) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_quota_result {
    pub quota: *mut novamem_quota_result_quota,
    pub user_id: *mut c_char,
}

impl novamem_quota_result {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::QuotaResult {
        types::QuotaResult {
            quota: self.quota.as_ref().map(|p| p.to_rust()).unwrap_or_default(),
            user_id: c_str(self.user_id).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::QuotaResult) -> Self {
        Self {
            quota: boxed(novamem_quota_result_quota::to_c(&v.quota)),
            user_id: str_to_c(Some(v.user_id.as_str())),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        novamem_quota_result_quota_free(self.quota);
        free_str(self.user_id);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_quota_result_free(p: *mut novamem_quota_result) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_quota_result_quota {
    pub has_max_entries: bool,
    pub max_entries: i32,
    pub has_writes_per_minute: bool,
    pub writes_per_minute: i32,
}

impl novamem_quota_result_quota {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::QuotaResultQuota {
        types::QuotaResultQuota {
            max_entries: if self.has_max_entries { Some(self.max_entries) } else { None },
            writes_per_minute: if self.has_writes_per_minute { Some(self.writes_per_minute) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::QuotaResultQuota) -> Self {
        Self {
            has_max_entries: v.max_entries.is_some(),
            max_entries: v.max_entries.unwrap_or_default(),
            has_writes_per_minute: v.writes_per_minute.is_some(),
            writes_per_minute: v.writes_per_minute.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {


    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_quota_result_quota_free(p: *mut novamem_quota_result_quota) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_recent_request {
    pub content_mode: *mut c_char,
    pub include_namespaces: *mut *mut c_char,
    pub include_namespaces_len: usize,
    pub include_projects: *mut *mut c_char,
    pub include_projects_len: usize,
    pub has_k: bool,
    pub k: i32,
    pub max_sensitivity: *mut c_char,
    pub namespace: *mut c_char,
    pub project: *mut c_char,
    pub since: *mut c_char,
}

impl novamem_recent_request {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::RecentRequest {
        types::RecentRequest {
            content_mode: c_enum(self.content_mode),
            include_namespaces: c_str_array(self.include_namespaces, self.include_namespaces_len),
            include_projects: c_str_array(self.include_projects, self.include_projects_len),
            k: if self.has_k { Some(self.k) } else { None },
            max_sensitivity: c_enum(self.max_sensitivity),
            namespace: c_str(self.namespace),
            project: c_str(self.project),
            since: c_str(self.since),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::RecentRequest) -> Self {
        let (include_namespaces_ptr, include_namespaces_len) = str_array_to_c(v.include_namespaces.as_deref());
        let (include_projects_ptr, include_projects_len) = str_array_to_c(v.include_projects.as_deref());
        Self {
            content_mode: enum_to_c(v.content_mode.as_ref()),
            include_namespaces: include_namespaces_ptr,
            include_namespaces_len,
            include_projects: include_projects_ptr,
            include_projects_len,
            has_k: v.k.is_some(),
            k: v.k.unwrap_or_default(),
            max_sensitivity: enum_to_c(v.max_sensitivity.as_ref()),
            namespace: str_to_c(v.namespace.as_deref()),
            project: str_to_c(v.project.as_deref()),
            since: str_to_c(v.since.as_deref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.content_mode);
        free_str_array(self.include_namespaces, self.include_namespaces_len);
        free_str_array(self.include_projects, self.include_projects_len);

        free_str(self.max_sensitivity);
        free_str(self.namespace);
        free_str(self.project);
        free_str(self.since);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_recent_request_free(p: *mut novamem_recent_request) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_remember_result {
    pub has_deduplicated: bool,
    pub deduplicated: bool,
    pub has_embedded: bool,
    pub embedded: bool,
    pub id: *mut c_char,
    pub rejected: *mut c_char,
    pub superseded: *mut *mut c_char,
    pub superseded_len: usize,
    pub has_updated: bool,
    pub updated: bool,
}

impl novamem_remember_result {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::RememberResult {
        types::RememberResult {
            deduplicated: if self.has_deduplicated { Some(self.deduplicated) } else { None },
            embedded: if self.has_embedded { Some(self.embedded) } else { None },
            id: c_str(self.id),
            rejected: c_str(self.rejected),
            superseded: c_str_array(self.superseded, self.superseded_len),
            updated: if self.has_updated { Some(self.updated) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::RememberResult) -> Self {
        let (superseded_ptr, superseded_len) = str_array_to_c(v.superseded.as_deref());
        Self {
            has_deduplicated: v.deduplicated.is_some(),
            deduplicated: v.deduplicated.unwrap_or_default(),
            has_embedded: v.embedded.is_some(),
            embedded: v.embedded.unwrap_or_default(),
            id: str_to_c(v.id.as_deref()),
            rejected: str_to_c(v.rejected.as_deref()),
            superseded: superseded_ptr,
            superseded_len,
            has_updated: v.updated.is_some(),
            updated: v.updated.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {


        free_str(self.id);
        free_str(self.rejected);
        free_str_array(self.superseded, self.superseded_len);

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_remember_result_free(p: *mut novamem_remember_result) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_revoke_result {
    pub revoked: bool,
}

impl novamem_revoke_result {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::RevokeResult {
        types::RevokeResult {
            revoked: self.revoked,
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::RevokeResult) -> Self {
        Self {
            revoked: v.revoked,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_revoke_result_free(p: *mut novamem_revoke_result) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_search_request {
    pub agent_name: *mut c_char,
    pub as_of: *mut c_char,
    pub content_mode: *mut c_char,
    pub has_decompose: bool,
    pub decompose: bool,
    pub has_expand_source_chunks: bool,
    pub expand_source_chunks: bool,
    pub include_namespaces: *mut *mut c_char,
    pub include_namespaces_len: usize,
    pub include_projects: *mut *mut c_char,
    pub include_projects_len: usize,
    pub has_k: bool,
    pub k: i32,
    pub max_sensitivity: *mut c_char,
    pub has_max_tokens: bool,
    pub max_tokens: i32,
    pub has_min_vector_score: bool,
    pub min_vector_score: f64,
    pub namespace: *mut c_char,
    pub project: *mut c_char,
    pub query: *mut c_char,
    pub has_rerank: bool,
    pub rerank: bool,
    pub weights: *mut novamem_search_request_weights,
}

impl novamem_search_request {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::SearchRequest {
        types::SearchRequest {
            agent_name: c_str(self.agent_name),
            as_of: c_str(self.as_of),
            content_mode: c_enum(self.content_mode),
            decompose: if self.has_decompose { Some(self.decompose) } else { None },
            expand_source_chunks: if self.has_expand_source_chunks { Some(self.expand_source_chunks) } else { None },
            include_namespaces: c_str_array(self.include_namespaces, self.include_namespaces_len),
            include_projects: c_str_array(self.include_projects, self.include_projects_len),
            k: if self.has_k { Some(self.k) } else { None },
            max_sensitivity: c_enum(self.max_sensitivity),
            max_tokens: if self.has_max_tokens { Some(self.max_tokens) } else { None },
            min_vector_score: if self.has_min_vector_score { Some(self.min_vector_score) } else { None },
            namespace: c_str(self.namespace),
            project: c_str(self.project),
            query: c_str(self.query).unwrap_or_default(),
            rerank: if self.has_rerank { Some(self.rerank) } else { None },
            weights: self.weights.as_ref().map(|p| p.to_rust()),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::SearchRequest) -> Self {
        let (include_namespaces_ptr, include_namespaces_len) = str_array_to_c(v.include_namespaces.as_deref());
        let (include_projects_ptr, include_projects_len) = str_array_to_c(v.include_projects.as_deref());
        Self {
            agent_name: str_to_c(v.agent_name.as_deref()),
            as_of: str_to_c(v.as_of.as_deref()),
            content_mode: enum_to_c(v.content_mode.as_ref()),
            has_decompose: v.decompose.is_some(),
            decompose: v.decompose.unwrap_or_default(),
            has_expand_source_chunks: v.expand_source_chunks.is_some(),
            expand_source_chunks: v.expand_source_chunks.unwrap_or_default(),
            include_namespaces: include_namespaces_ptr,
            include_namespaces_len,
            include_projects: include_projects_ptr,
            include_projects_len,
            has_k: v.k.is_some(),
            k: v.k.unwrap_or_default(),
            max_sensitivity: enum_to_c(v.max_sensitivity.as_ref()),
            has_max_tokens: v.max_tokens.is_some(),
            max_tokens: v.max_tokens.unwrap_or_default(),
            has_min_vector_score: v.min_vector_score.is_some(),
            min_vector_score: v.min_vector_score.unwrap_or_default(),
            namespace: str_to_c(v.namespace.as_deref()),
            project: str_to_c(v.project.as_deref()),
            query: str_to_c(Some(v.query.as_str())),
            has_rerank: v.rerank.is_some(),
            rerank: v.rerank.unwrap_or_default(),
            weights: v.weights.as_ref().map_or(std::ptr::null_mut(), |x| boxed(novamem_search_request_weights::to_c(x))),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.agent_name);
        free_str(self.as_of);
        free_str(self.content_mode);


        free_str_array(self.include_namespaces, self.include_namespaces_len);
        free_str_array(self.include_projects, self.include_projects_len);

        free_str(self.max_sensitivity);


        free_str(self.namespace);
        free_str(self.project);
        free_str(self.query);

        novamem_search_request_weights_free(self.weights);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_search_request_free(p: *mut novamem_search_request) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_search_request_weights {
    pub has_entity: bool,
    pub entity: f64,
    pub has_graph: bool,
    pub graph: f64,
    pub has_keyword: bool,
    pub keyword: f64,
    pub has_recency: bool,
    pub recency: f64,
    pub has_vector: bool,
    pub vector: f64,
}

impl novamem_search_request_weights {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::SearchRequestWeights {
        types::SearchRequestWeights {
            entity: if self.has_entity { Some(self.entity) } else { None },
            graph: if self.has_graph { Some(self.graph) } else { None },
            keyword: if self.has_keyword { Some(self.keyword) } else { None },
            recency: if self.has_recency { Some(self.recency) } else { None },
            vector: if self.has_vector { Some(self.vector) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::SearchRequestWeights) -> Self {
        Self {
            has_entity: v.entity.is_some(),
            entity: v.entity.unwrap_or_default(),
            has_graph: v.graph.is_some(),
            graph: v.graph.unwrap_or_default(),
            has_keyword: v.keyword.is_some(),
            keyword: v.keyword.unwrap_or_default(),
            has_recency: v.recency.is_some(),
            recency: v.recency.unwrap_or_default(),
            has_vector: v.vector.is_some(),
            vector: v.vector.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {





    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_search_request_weights_free(p: *mut novamem_search_request_weights) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_search_result {
    pub has_degraded: bool,
    pub degraded: bool,
    pub results: *mut novamem_memory_entry,
    pub results_len: usize,
}

impl novamem_search_result {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::SearchResult {
        types::SearchResult {
            degraded: if self.has_degraded { Some(self.degraded) } else { None },
            results: c_obj_array(self.results, self.results_len, |x| x.to_rust()).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::SearchResult) -> Self {
        let (results_ptr, results_len) = obj_array_to_c(Some(&v.results[..]), novamem_memory_entry::to_c);
        Self {
            has_degraded: v.degraded.is_some(),
            degraded: v.degraded.unwrap_or_default(),
            results: results_ptr,
            results_len,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {

        free_obj_array(self.results, self.results_len, |x| x.free_fields());
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_search_result_free(p: *mut novamem_search_result) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_session_recap_request {
    pub agent_name: *mut c_char,
    pub captured_from: *mut c_char,
    pub has_confidence: bool,
    pub confidence: f64,
    pub decisions: *mut *mut c_char,
    pub decisions_len: usize,
    pub has_force: bool,
    pub force: bool,
    pub metadata: *mut c_char,
    pub namespace: *mut c_char,
    pub other: *mut *mut c_char,
    pub other_len: usize,
    pub preferences: *mut *mut c_char,
    pub preferences_len: usize,
    pub project: *mut c_char,
    pub project_conventions: *mut *mut c_char,
    pub project_conventions_len: usize,
    pub root_causes: *mut *mut c_char,
    pub root_causes_len: usize,
    pub safety_constraints: *mut *mut c_char,
    pub safety_constraints_len: usize,
    pub sensitivity: *mut c_char,
    pub setup_facts: *mut *mut c_char,
    pub setup_facts_len: usize,
    pub source: *mut c_char,
    pub source_type: *mut c_char,
}

impl novamem_session_recap_request {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::SessionRecapRequest {
        types::SessionRecapRequest {
            agent_name: c_str(self.agent_name),
            captured_from: c_str(self.captured_from),
            confidence: if self.has_confidence { Some(self.confidence) } else { None },
            decisions: c_str_array(self.decisions, self.decisions_len),
            force: if self.has_force { Some(self.force) } else { None },
            metadata: c_json(self.metadata),
            namespace: c_str(self.namespace),
            other: c_str_array(self.other, self.other_len),
            preferences: c_str_array(self.preferences, self.preferences_len),
            project: c_str(self.project),
            project_conventions: c_str_array(self.project_conventions, self.project_conventions_len),
            root_causes: c_str_array(self.root_causes, self.root_causes_len),
            safety_constraints: c_str_array(self.safety_constraints, self.safety_constraints_len),
            sensitivity: c_enum(self.sensitivity),
            setup_facts: c_str_array(self.setup_facts, self.setup_facts_len),
            source: c_str(self.source),
            source_type: c_str(self.source_type),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::SessionRecapRequest) -> Self {
        let (decisions_ptr, decisions_len) = str_array_to_c(v.decisions.as_deref());
        let (other_ptr, other_len) = str_array_to_c(v.other.as_deref());
        let (preferences_ptr, preferences_len) = str_array_to_c(v.preferences.as_deref());
        let (project_conventions_ptr, project_conventions_len) = str_array_to_c(v.project_conventions.as_deref());
        let (root_causes_ptr, root_causes_len) = str_array_to_c(v.root_causes.as_deref());
        let (safety_constraints_ptr, safety_constraints_len) = str_array_to_c(v.safety_constraints.as_deref());
        let (setup_facts_ptr, setup_facts_len) = str_array_to_c(v.setup_facts.as_deref());
        Self {
            agent_name: str_to_c(v.agent_name.as_deref()),
            captured_from: str_to_c(v.captured_from.as_deref()),
            has_confidence: v.confidence.is_some(),
            confidence: v.confidence.unwrap_or_default(),
            decisions: decisions_ptr,
            decisions_len,
            has_force: v.force.is_some(),
            force: v.force.unwrap_or_default(),
            metadata: json_to_c(v.metadata.as_ref()),
            namespace: str_to_c(v.namespace.as_deref()),
            other: other_ptr,
            other_len,
            preferences: preferences_ptr,
            preferences_len,
            project: str_to_c(v.project.as_deref()),
            project_conventions: project_conventions_ptr,
            project_conventions_len,
            root_causes: root_causes_ptr,
            root_causes_len,
            safety_constraints: safety_constraints_ptr,
            safety_constraints_len,
            sensitivity: enum_to_c(v.sensitivity.as_ref()),
            setup_facts: setup_facts_ptr,
            setup_facts_len,
            source: str_to_c(v.source.as_deref()),
            source_type: str_to_c(v.source_type.as_deref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.agent_name);
        free_str(self.captured_from);

        free_str_array(self.decisions, self.decisions_len);

        free_str(self.metadata);
        free_str(self.namespace);
        free_str_array(self.other, self.other_len);
        free_str_array(self.preferences, self.preferences_len);
        free_str(self.project);
        free_str_array(self.project_conventions, self.project_conventions_len);
        free_str_array(self.root_causes, self.root_causes_len);
        free_str_array(self.safety_constraints, self.safety_constraints_len);
        free_str(self.sensitivity);
        free_str_array(self.setup_facts, self.setup_facts_len);
        free_str(self.source);
        free_str(self.source_type);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_session_recap_request_free(p: *mut novamem_session_recap_request) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_session_recap_result {
    pub results: *mut novamem_capture_result,
    pub results_len: usize,
    pub saved: f64,
}

impl novamem_session_recap_result {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::SessionRecapResult {
        types::SessionRecapResult {
            results: c_obj_array(self.results, self.results_len, |x| x.to_rust()).unwrap_or_default(),
            saved: self.saved,
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::SessionRecapResult) -> Self {
        let (results_ptr, results_len) = obj_array_to_c(Some(&v.results[..]), novamem_capture_result::to_c);
        Self {
            results: results_ptr,
            results_len,
            saved: v.saved,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_obj_array(self.results, self.results_len, |x| x.free_fields());

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_session_recap_result_free(p: *mut novamem_session_recap_result) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_stats {
    pub by_namespace: *mut c_char,
    pub last_decay_at: *mut c_char,
    pub total_cold: f64,
    pub total_warm: f64,
    pub has_uptime_ms: bool,
    pub uptime_ms: f64,
}

impl novamem_stats {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::Stats {
        types::Stats {
            by_namespace: c_json(self.by_namespace).unwrap_or_default(),
            last_decay_at: c_str(self.last_decay_at),
            total_cold: self.total_cold,
            total_warm: self.total_warm,
            uptime_ms: if self.has_uptime_ms { Some(self.uptime_ms) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::Stats) -> Self {
        Self {
            by_namespace: json_to_c(Some(&v.by_namespace)),
            last_decay_at: str_to_c(v.last_decay_at.as_deref()),
            total_cold: v.total_cold,
            total_warm: v.total_warm,
            has_uptime_ms: v.uptime_ms.is_some(),
            uptime_ms: v.uptime_ms.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.by_namespace);
        free_str(self.last_decay_at);



    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_stats_free(p: *mut novamem_stats) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_stats_by_namespace_value {
    pub has_cold: bool,
    pub cold: f64,
    pub has_warm: bool,
    pub warm: f64,
}

impl novamem_stats_by_namespace_value {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::StatsByNamespaceValue {
        types::StatsByNamespaceValue {
            cold: if self.has_cold { Some(self.cold) } else { None },
            warm: if self.has_warm { Some(self.warm) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::StatsByNamespaceValue) -> Self {
        Self {
            has_cold: v.cold.is_some(),
            cold: v.cold.unwrap_or_default(),
            has_warm: v.warm.is_some(),
            warm: v.warm.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {


    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_stats_by_namespace_value_free(p: *mut novamem_stats_by_namespace_value) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_token_deleted {
    pub deleted: bool,
}

impl novamem_token_deleted {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::TokenDeleted {
        types::TokenDeleted {
            deleted: self.deleted,
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::TokenDeleted) -> Self {
        Self {
            deleted: v.deleted,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_token_deleted_free(p: *mut novamem_token_deleted) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_token_list {
    pub tokens: *mut c_char,
}

impl novamem_token_list {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::TokenList {
        types::TokenList {
            tokens: c_json(self.tokens).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::TokenList) -> Self {
        Self {
            tokens: json_to_c(Some(&v.tokens)),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.tokens);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_token_list_free(p: *mut novamem_token_list) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_update_request {
    pub captured_from: *mut c_char,
    pub has_confidence: bool,
    pub confidence: f64,
    pub content: *mut c_char,
    pub metadata: *mut c_char,
    pub namespace: *mut c_char,
    pub project: *mut c_char,
    pub sensitivity: *mut c_char,
    pub source_type: *mut c_char,
}

impl novamem_update_request {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::UpdateRequest {
        types::UpdateRequest {
            captured_from: c_str(self.captured_from),
            confidence: if self.has_confidence { Some(self.confidence) } else { None },
            content: c_str(self.content),
            metadata: c_json(self.metadata),
            namespace: c_str(self.namespace),
            project: c_str(self.project),
            sensitivity: c_enum(self.sensitivity),
            source_type: c_str(self.source_type),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::UpdateRequest) -> Self {
        Self {
            captured_from: str_to_c(v.captured_from.as_deref()),
            has_confidence: v.confidence.is_some(),
            confidence: v.confidence.unwrap_or_default(),
            content: str_to_c(v.content.as_deref()),
            metadata: json_to_c(v.metadata.as_ref()),
            namespace: str_to_c(v.namespace.as_deref()),
            project: str_to_c(v.project.as_deref()),
            sensitivity: enum_to_c(v.sensitivity.as_ref()),
            source_type: str_to_c(v.source_type.as_deref()),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.captured_from);

        free_str(self.content);
        free_str(self.metadata);
        free_str(self.namespace);
        free_str(self.project);
        free_str(self.sensitivity);
        free_str(self.source_type);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_update_request_free(p: *mut novamem_update_request) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_update_result {
    pub embedding_changed: bool,
    pub id: *mut c_char,
    pub updated: bool,
}

impl novamem_update_result {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::UpdateResult {
        types::UpdateResult {
            embedding_changed: self.embedding_changed,
            id: c_str(self.id).unwrap_or_default(),
            updated: self.updated,
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::UpdateResult) -> Self {
        Self {
            embedding_changed: v.embedding_changed,
            id: str_to_c(Some(v.id.as_str())),
            updated: v.updated,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {

        free_str(self.id);

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_update_result_free(p: *mut novamem_update_result) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_usage {
    pub has_entries: bool,
    pub entries: f64,
    pub quota: *mut novamem_usage_quota,
}

impl novamem_usage {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::Usage {
        types::Usage {
            entries: if self.has_entries { Some(self.entries) } else { None },
            quota: self.quota.as_ref().map(|p| p.to_rust()),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::Usage) -> Self {
        Self {
            has_entries: v.entries.is_some(),
            entries: v.entries.unwrap_or_default(),
            quota: v.quota.as_ref().map_or(std::ptr::null_mut(), |x| boxed(novamem_usage_quota::to_c(x))),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {

        novamem_usage_quota_free(self.quota);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_usage_free(p: *mut novamem_usage) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_usage_quota {
    pub has_max_entries: bool,
    pub max_entries: f64,
    pub has_writes_per_minute: bool,
    pub writes_per_minute: f64,
}

impl novamem_usage_quota {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::UsageQuota {
        types::UsageQuota {
            max_entries: if self.has_max_entries { Some(self.max_entries) } else { None },
            writes_per_minute: if self.has_writes_per_minute { Some(self.writes_per_minute) } else { None },
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::UsageQuota) -> Self {
        Self {
            has_max_entries: v.max_entries.is_some(),
            max_entries: v.max_entries.unwrap_or_default(),
            has_writes_per_minute: v.writes_per_minute.is_some(),
            writes_per_minute: v.writes_per_minute.unwrap_or_default(),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {


    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_usage_quota_free(p: *mut novamem_usage_quota) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_user_deletion {
    pub cold_cleanup: *mut *mut c_char,
    pub cold_cleanup_len: usize,
    pub cold_cleanup_ok: bool,
    pub deleted: bool,
    pub entries_removed: i32,
    pub projects_deleted: *mut *mut c_char,
    pub projects_deleted_len: usize,
    pub tokens_removed: i32,
}

impl novamem_user_deletion {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::UserDeletion {
        types::UserDeletion {
            cold_cleanup: c_str_array(self.cold_cleanup, self.cold_cleanup_len).unwrap_or_default(),
            cold_cleanup_ok: self.cold_cleanup_ok,
            deleted: self.deleted,
            entries_removed: self.entries_removed,
            projects_deleted: c_str_array(self.projects_deleted, self.projects_deleted_len).unwrap_or_default(),
            tokens_removed: self.tokens_removed,
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::UserDeletion) -> Self {
        let (cold_cleanup_ptr, cold_cleanup_len) = str_array_to_c(Some(&v.cold_cleanup[..]));
        let (projects_deleted_ptr, projects_deleted_len) = str_array_to_c(Some(&v.projects_deleted[..]));
        Self {
            cold_cleanup: cold_cleanup_ptr,
            cold_cleanup_len,
            cold_cleanup_ok: v.cold_cleanup_ok,
            deleted: v.deleted,
            entries_removed: v.entries_removed,
            projects_deleted: projects_deleted_ptr,
            projects_deleted_len,
            tokens_removed: v.tokens_removed,
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str_array(self.cold_cleanup, self.cold_cleanup_len);



        free_str_array(self.projects_deleted, self.projects_deleted_len);

    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_user_deletion_free(p: *mut novamem_user_deletion) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_user_deletion_preview {
    pub dry_run: bool,
    pub would_delete: *mut novamem_user_deletion_preview_would_delete,
}

impl novamem_user_deletion_preview {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::UserDeletionPreview {
        types::UserDeletionPreview {
            dry_run: self.dry_run,
            would_delete: self.would_delete.as_ref().map(|p| p.to_rust()).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::UserDeletionPreview) -> Self {
        Self {
            dry_run: v.dry_run,
            would_delete: boxed(novamem_user_deletion_preview_would_delete::to_c(&v.would_delete)),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {

        novamem_user_deletion_preview_would_delete_free(self.would_delete);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_user_deletion_preview_free(p: *mut novamem_user_deletion_preview) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}

#[repr(C)]
pub struct novamem_user_deletion_preview_would_delete {
    pub email: *mut c_char,
    pub entries: i32,
    pub owned_projects: *mut *mut c_char,
    pub owned_projects_len: usize,
    pub tokens: i32,
    pub user_id: *mut c_char,
}

impl novamem_user_deletion_preview_would_delete {
    /// Reads a C-owned value into the SDK type.
    pub(crate) unsafe fn to_rust(&self) -> types::UserDeletionPreviewWouldDelete {
        types::UserDeletionPreviewWouldDelete {
            email: c_str(self.email),
            entries: self.entries,
            owned_projects: c_str_array(self.owned_projects, self.owned_projects_len).unwrap_or_default(),
            tokens: self.tokens,
            user_id: c_str(self.user_id).unwrap_or_default(),
        }
    }

    /// Builds a C value that owns copies of everything in `v`.
    pub(crate) fn to_c(v: &types::UserDeletionPreviewWouldDelete) -> Self {
        let (owned_projects_ptr, owned_projects_len) = str_array_to_c(Some(&v.owned_projects[..]));
        Self {
            email: str_to_c(v.email.as_deref()),
            entries: v.entries,
            owned_projects: owned_projects_ptr,
            owned_projects_len,
            tokens: v.tokens,
            user_id: str_to_c(Some(v.user_id.as_str())),
        }
    }

    /// Frees everything this value owns, but not the value itself.
    pub(crate) unsafe fn free_fields(&mut self) {
        free_str(self.email);

        free_str_array(self.owned_projects, self.owned_projects_len);

        free_str(self.user_id);
    }
}

/// Frees a value returned by this library, and everything inside it.
#[no_mangle]
pub unsafe extern "C" fn novamem_user_deletion_preview_would_delete_free(p: *mut novamem_user_deletion_preview_would_delete) {
    if !p.is_null() {
        let mut b = Box::from_raw(p);
        b.free_fields();
    }
}
