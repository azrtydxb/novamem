# novamem — C and C++ client

A C ABI (`include/novamem.h`) and a header-only C++17 wrapper (`include/novamem.hpp`) over the [Rust SDK](../rust), for [novamem](https://github.com/azrtydxb/novamem).

The full README (build, quickstart, error contract, operations table) lands with plan Task 18.
