{
  "claude-code": {
    "tool": "claude-code",
    "files": {
      ".claude/commands/forget.md": "---\ndescription: Permanently delete a NovaMem entry\nargument-hint: <entry-id>\nallowed-tools: mcp__novamem__memory_forget\n---\n\nCall `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\nthe warm row, the FTS index, the cold vector, and graph edges — it is\nnot recoverable. Confirm in one short line whether deletion succeeded.\n",
      ".claude/commands/memory-stats.md": "---\ndescription: NovaMem stats snapshot (counts + last decay)\nallowed-tools: mcp__novamem__memory_stats\n---\n\nCall `mcp__novamem__memory_stats`. Render as a compact 4-line block:\nwarm / cold / graph counts, last decay run.\n",
      ".claude/commands/neighbors.md": "---\ndescription: Graph-neighbour traversal from a NovaMem entry id\nargument-hint: <seed-id> [depth=1] [k=10]\nallowed-tools: mcp__novamem__memory_neighbors\n---\n\nCall `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\nuser passed extra positional args, parse them as `depth` and `k` (both\nintegers). Default `depth: 1, k: 10`.\n\nTraversal runs over the server's SQL relations table; if that query\nfails, the response carries `degraded: true` — surface that briefly.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".claude/commands/project-create.md": "---\ndescription: Create a new NovaMem project (sub-brain)\nargument-hint: <id> <display-name>\nallowed-tools: mcp__novamem__project_create\n---\n\nParse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\nnew project record. After creation, future `/remember` and `/recall`\ncalls scoped to this id will land in the new sub-brain.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".claude/commands/projects.md": "---\ndescription: List NovaMem projects (sub-brains) you can access\nallowed-tools: mcp__novamem__project_list\n---\n\nCall `mcp__novamem__project_list`. Render each project as\n`id  name  role  members` on its own line. If there are zero, suggest\n`/project-create <id> <name>` to start a sub-brain.\n",
      ".claude/commands/recall.md": "---\ndescription: 5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\nargument-hint: <query>\nallowed-tools: mcp__novamem__memory_search\n---\n\nSearch NovaMem by calling `mcp__novamem__memory_search` with the query\nbelow. Use defaults unless the query strongly hints otherwise:\n\n- If the query is a literal id, symbol, or hash, pass\n  `weights: { keyword: 1, vector: 0 }`.\n- If the user asks for \"anything related to / similar to / like X\",\n  pass `weights: { vector: 1, keyword: 0 }`.\n\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\n1-line content excerpt. If nothing in the returned content actually answers the\nquery, say \"no strong matches\" and stop — don't pad with irrelevant hits. Don't\ngate on an absolute score: the usable range is model-dependent.\n\nQUERY:\n$ARGUMENTS\n",
      ".claude/commands/recent.md": "---\ndescription: Newest NovaMem entries (optionally in a namespace)\nargument-hint: [namespace]\nallowed-tools: mcp__novamem__memory_recent\n---\n\nCall `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\n`id  age  ns  one-line content`.\n",
      ".claude/commands/remember.md": "---\ndescription: Save a memory to NovaMem (durable across sessions)\nargument-hint: <content to remember>\nallowed-tools: mcp__novamem__memory_remember\n---\n\nStore the following as a NovaMem memory entry by calling\n`mcp__novamem__memory_remember`. If the user is currently working in a\nspecific project (visible in their recent context), pass `project: <id>`\nto scope it; otherwise leave project unset for a user-wide entry.\n\nAfter saving, reply in one short sentence with the assigned id and a\none-line summary of what was saved, so the user can edit or `/forget`\nif needed.\n\nCONTENT TO REMEMBER:\n$ARGUMENTS\n",
      ".claude/commands/today.md": "---\ndescription: NovaMem activity in the last 24h\nallowed-tools: mcp__novamem__memory_today\n---\n\nCall `mcp__novamem__memory_today` (no args). Render the result as a\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\nthere are more than 10 events.\n",
      ".mcp.json": "{\n  \"mcpServers\": {\n    \"other\": {\n      \"type\": \"sse\",\n      \"url\": \"https://other\"\n    },\n    \"novamem\": {\n      \"type\": \"sse\",\n      \"url\": \"https://memory.example.com/mcp/sse\",\n      \"headers\": {\n        \"Authorization\": \"Bearer nm_GOLDENFIXTURETOKEN\"\n      }\n    }\n  },\n  \"editorSetting\": 42\n}\n"
    }
  },
  "claude-desktop": {
    "tool": "claude-desktop",
    "files": {
      "Library/Application Support/Claude/claude_desktop_config.json": "{\n  \"mcpServers\": {\n    \"other\": {\n      \"type\": \"sse\",\n      \"url\": \"https://other\"\n    },\n    \"novamem\": {\n      \"command\": \"npx\",\n      \"args\": [\n        \"-y\",\n        \"@azrtydxb/novamem-mcp@9.9.9\"\n      ],\n      \"env\": {\n        \"NOVAMEM_BASE_URL\": \"https://memory.example.com\",\n        \"NOVAMEM_TOKEN\": \"nm_GOLDENFIXTURETOKEN\"\n      }\n    }\n  },\n  \"editorSetting\": 42\n}\n"
    }
  },
  "cursor": {
    "tool": "cursor",
    "files": {
      ".cursor/commands/forget.md": "---\ndescription: Permanently delete a NovaMem entry\nargument-hint: <entry-id>\nallowed-tools: mcp__novamem__memory_forget\n---\n\nCall `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\nthe warm row, the FTS index, the cold vector, and graph edges — it is\nnot recoverable. Confirm in one short line whether deletion succeeded.\n",
      ".cursor/commands/memory-stats.md": "---\ndescription: NovaMem stats snapshot (counts + last decay)\nallowed-tools: mcp__novamem__memory_stats\n---\n\nCall `mcp__novamem__memory_stats`. Render as a compact 4-line block:\nwarm / cold / graph counts, last decay run.\n",
      ".cursor/commands/neighbors.md": "---\ndescription: Graph-neighbour traversal from a NovaMem entry id\nargument-hint: <seed-id> [depth=1] [k=10]\nallowed-tools: mcp__novamem__memory_neighbors\n---\n\nCall `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\nuser passed extra positional args, parse them as `depth` and `k` (both\nintegers). Default `depth: 1, k: 10`.\n\nTraversal runs over the server's SQL relations table; if that query\nfails, the response carries `degraded: true` — surface that briefly.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".cursor/commands/project-create.md": "---\ndescription: Create a new NovaMem project (sub-brain)\nargument-hint: <id> <display-name>\nallowed-tools: mcp__novamem__project_create\n---\n\nParse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\nnew project record. After creation, future `/remember` and `/recall`\ncalls scoped to this id will land in the new sub-brain.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".cursor/commands/projects.md": "---\ndescription: List NovaMem projects (sub-brains) you can access\nallowed-tools: mcp__novamem__project_list\n---\n\nCall `mcp__novamem__project_list`. Render each project as\n`id  name  role  members` on its own line. If there are zero, suggest\n`/project-create <id> <name>` to start a sub-brain.\n",
      ".cursor/commands/recall.md": "---\ndescription: 5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\nargument-hint: <query>\nallowed-tools: mcp__novamem__memory_search\n---\n\nSearch NovaMem by calling `mcp__novamem__memory_search` with the query\nbelow. Use defaults unless the query strongly hints otherwise:\n\n- If the query is a literal id, symbol, or hash, pass\n  `weights: { keyword: 1, vector: 0 }`.\n- If the user asks for \"anything related to / similar to / like X\",\n  pass `weights: { vector: 1, keyword: 0 }`.\n\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\n1-line content excerpt. If nothing in the returned content actually answers the\nquery, say \"no strong matches\" and stop — don't pad with irrelevant hits. Don't\ngate on an absolute score: the usable range is model-dependent.\n\nQUERY:\n$ARGUMENTS\n",
      ".cursor/commands/recent.md": "---\ndescription: Newest NovaMem entries (optionally in a namespace)\nargument-hint: [namespace]\nallowed-tools: mcp__novamem__memory_recent\n---\n\nCall `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\n`id  age  ns  one-line content`.\n",
      ".cursor/commands/remember.md": "---\ndescription: Save a memory to NovaMem (durable across sessions)\nargument-hint: <content to remember>\nallowed-tools: mcp__novamem__memory_remember\n---\n\nStore the following as a NovaMem memory entry by calling\n`mcp__novamem__memory_remember`. If the user is currently working in a\nspecific project (visible in their recent context), pass `project: <id>`\nto scope it; otherwise leave project unset for a user-wide entry.\n\nAfter saving, reply in one short sentence with the assigned id and a\none-line summary of what was saved, so the user can edit or `/forget`\nif needed.\n\nCONTENT TO REMEMBER:\n$ARGUMENTS\n",
      ".cursor/commands/today.md": "---\ndescription: NovaMem activity in the last 24h\nallowed-tools: mcp__novamem__memory_today\n---\n\nCall `mcp__novamem__memory_today` (no args). Render the result as a\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\nthere are more than 10 events.\n",
      ".cursor/mcp.json": "{\n  \"mcpServers\": {\n    \"other\": {\n      \"type\": \"sse\",\n      \"url\": \"https://other\"\n    },\n    \"novamem\": {\n      \"type\": \"sse\",\n      \"url\": \"https://memory.example.com/mcp/sse\",\n      \"headers\": {\n        \"Authorization\": \"Bearer nm_GOLDENFIXTURETOKEN\"\n      }\n    }\n  },\n  \"editorSetting\": 42\n}\n"
    }
  },
  "kilocode": {
    "tool": "kilocode",
    "files": {
      ".kilocode/mcp.json": "{\n  \"mcpServers\": {\n    \"other\": {\n      \"type\": \"sse\",\n      \"url\": \"https://other\"\n    },\n    \"novamem\": {\n      \"type\": \"sse\",\n      \"url\": \"https://memory.example.com/mcp/sse\",\n      \"headers\": {\n        \"Authorization\": \"Bearer nm_GOLDENFIXTURETOKEN\"\n      }\n    }\n  },\n  \"editorSetting\": 42\n}\n",
      ".kilocode/workflows/forget.md": "---\ndescription: Permanently delete a NovaMem entry\nargument-hint: <entry-id>\nallowed-tools: mcp__novamem__memory_forget\n---\n\nCall `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\nthe warm row, the FTS index, the cold vector, and graph edges — it is\nnot recoverable. Confirm in one short line whether deletion succeeded.\n",
      ".kilocode/workflows/memory-stats.md": "---\ndescription: NovaMem stats snapshot (counts + last decay)\nallowed-tools: mcp__novamem__memory_stats\n---\n\nCall `mcp__novamem__memory_stats`. Render as a compact 4-line block:\nwarm / cold / graph counts, last decay run.\n",
      ".kilocode/workflows/neighbors.md": "---\ndescription: Graph-neighbour traversal from a NovaMem entry id\nargument-hint: <seed-id> [depth=1] [k=10]\nallowed-tools: mcp__novamem__memory_neighbors\n---\n\nCall `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\nuser passed extra positional args, parse them as `depth` and `k` (both\nintegers). Default `depth: 1, k: 10`.\n\nTraversal runs over the server's SQL relations table; if that query\nfails, the response carries `degraded: true` — surface that briefly.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".kilocode/workflows/project-create.md": "---\ndescription: Create a new NovaMem project (sub-brain)\nargument-hint: <id> <display-name>\nallowed-tools: mcp__novamem__project_create\n---\n\nParse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\nnew project record. After creation, future `/remember` and `/recall`\ncalls scoped to this id will land in the new sub-brain.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".kilocode/workflows/projects.md": "---\ndescription: List NovaMem projects (sub-brains) you can access\nallowed-tools: mcp__novamem__project_list\n---\n\nCall `mcp__novamem__project_list`. Render each project as\n`id  name  role  members` on its own line. If there are zero, suggest\n`/project-create <id> <name>` to start a sub-brain.\n",
      ".kilocode/workflows/recall.md": "---\ndescription: 5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\nargument-hint: <query>\nallowed-tools: mcp__novamem__memory_search\n---\n\nSearch NovaMem by calling `mcp__novamem__memory_search` with the query\nbelow. Use defaults unless the query strongly hints otherwise:\n\n- If the query is a literal id, symbol, or hash, pass\n  `weights: { keyword: 1, vector: 0 }`.\n- If the user asks for \"anything related to / similar to / like X\",\n  pass `weights: { vector: 1, keyword: 0 }`.\n\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\n1-line content excerpt. If nothing in the returned content actually answers the\nquery, say \"no strong matches\" and stop — don't pad with irrelevant hits. Don't\ngate on an absolute score: the usable range is model-dependent.\n\nQUERY:\n$ARGUMENTS\n",
      ".kilocode/workflows/recent.md": "---\ndescription: Newest NovaMem entries (optionally in a namespace)\nargument-hint: [namespace]\nallowed-tools: mcp__novamem__memory_recent\n---\n\nCall `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\n`id  age  ns  one-line content`.\n",
      ".kilocode/workflows/remember.md": "---\ndescription: Save a memory to NovaMem (durable across sessions)\nargument-hint: <content to remember>\nallowed-tools: mcp__novamem__memory_remember\n---\n\nStore the following as a NovaMem memory entry by calling\n`mcp__novamem__memory_remember`. If the user is currently working in a\nspecific project (visible in their recent context), pass `project: <id>`\nto scope it; otherwise leave project unset for a user-wide entry.\n\nAfter saving, reply in one short sentence with the assigned id and a\none-line summary of what was saved, so the user can edit or `/forget`\nif needed.\n\nCONTENT TO REMEMBER:\n$ARGUMENTS\n",
      ".kilocode/workflows/today.md": "---\ndescription: NovaMem activity in the last 24h\nallowed-tools: mcp__novamem__memory_today\n---\n\nCall `mcp__novamem__memory_today` (no args). Render the result as a\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\nthere are more than 10 events.\n"
    }
  },
  "opencode": {
    "tool": "opencode",
    "files": {
      ".opencode/commands/forget.md": "---\ndescription: Permanently delete a NovaMem entry\nargument-hint: <entry-id>\nallowed-tools: mcp__novamem__memory_forget\n---\n\nCall `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\nthe warm row, the FTS index, the cold vector, and graph edges — it is\nnot recoverable. Confirm in one short line whether deletion succeeded.\n",
      ".opencode/commands/memory-stats.md": "---\ndescription: NovaMem stats snapshot (counts + last decay)\nallowed-tools: mcp__novamem__memory_stats\n---\n\nCall `mcp__novamem__memory_stats`. Render as a compact 4-line block:\nwarm / cold / graph counts, last decay run.\n",
      ".opencode/commands/neighbors.md": "---\ndescription: Graph-neighbour traversal from a NovaMem entry id\nargument-hint: <seed-id> [depth=1] [k=10]\nallowed-tools: mcp__novamem__memory_neighbors\n---\n\nCall `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\nuser passed extra positional args, parse them as `depth` and `k` (both\nintegers). Default `depth: 1, k: 10`.\n\nTraversal runs over the server's SQL relations table; if that query\nfails, the response carries `degraded: true` — surface that briefly.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".opencode/commands/project-create.md": "---\ndescription: Create a new NovaMem project (sub-brain)\nargument-hint: <id> <display-name>\nallowed-tools: mcp__novamem__project_create\n---\n\nParse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\nnew project record. After creation, future `/remember` and `/recall`\ncalls scoped to this id will land in the new sub-brain.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".opencode/commands/projects.md": "---\ndescription: List NovaMem projects (sub-brains) you can access\nallowed-tools: mcp__novamem__project_list\n---\n\nCall `mcp__novamem__project_list`. Render each project as\n`id  name  role  members` on its own line. If there are zero, suggest\n`/project-create <id> <name>` to start a sub-brain.\n",
      ".opencode/commands/recall.md": "---\ndescription: 5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\nargument-hint: <query>\nallowed-tools: mcp__novamem__memory_search\n---\n\nSearch NovaMem by calling `mcp__novamem__memory_search` with the query\nbelow. Use defaults unless the query strongly hints otherwise:\n\n- If the query is a literal id, symbol, or hash, pass\n  `weights: { keyword: 1, vector: 0 }`.\n- If the user asks for \"anything related to / similar to / like X\",\n  pass `weights: { vector: 1, keyword: 0 }`.\n\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\n1-line content excerpt. If nothing in the returned content actually answers the\nquery, say \"no strong matches\" and stop — don't pad with irrelevant hits. Don't\ngate on an absolute score: the usable range is model-dependent.\n\nQUERY:\n$ARGUMENTS\n",
      ".opencode/commands/recent.md": "---\ndescription: Newest NovaMem entries (optionally in a namespace)\nargument-hint: [namespace]\nallowed-tools: mcp__novamem__memory_recent\n---\n\nCall `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\n`id  age  ns  one-line content`.\n",
      ".opencode/commands/remember.md": "---\ndescription: Save a memory to NovaMem (durable across sessions)\nargument-hint: <content to remember>\nallowed-tools: mcp__novamem__memory_remember\n---\n\nStore the following as a NovaMem memory entry by calling\n`mcp__novamem__memory_remember`. If the user is currently working in a\nspecific project (visible in their recent context), pass `project: <id>`\nto scope it; otherwise leave project unset for a user-wide entry.\n\nAfter saving, reply in one short sentence with the assigned id and a\none-line summary of what was saved, so the user can edit or `/forget`\nif needed.\n\nCONTENT TO REMEMBER:\n$ARGUMENTS\n",
      ".opencode/commands/today.md": "---\ndescription: NovaMem activity in the last 24h\nallowed-tools: mcp__novamem__memory_today\n---\n\nCall `mcp__novamem__memory_today` (no args). Render the result as a\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\nthere are more than 10 events.\n",
      ".opencode/opencode.json": "{\n  \"mcp\": {\n    \"other\": {\n      \"type\": \"sse\",\n      \"url\": \"https://other\"\n    },\n    \"novamem\": {\n      \"command\": \"npx\",\n      \"args\": [\n        \"-y\",\n        \"@azrtydxb/novamem-mcp@9.9.9\"\n      ],\n      \"environment\": {\n        \"NOVAMEM_BASE_URL\": \"https://memory.example.com\",\n        \"NOVAMEM_TOKEN\": \"nm_GOLDENFIXTURETOKEN\"\n      },\n      \"type\": \"local\"\n    }\n  },\n  \"editorSetting\": 42\n}\n"
    }
  },
  "roocode": {
    "tool": "roocode",
    "files": {
      ".roo/commands/forget.md": "---\ndescription: Permanently delete a NovaMem entry\nargument-hint: <entry-id>\nallowed-tools: mcp__novamem__memory_forget\n---\n\nCall `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\nthe warm row, the FTS index, the cold vector, and graph edges — it is\nnot recoverable. Confirm in one short line whether deletion succeeded.\n",
      ".roo/commands/memory-stats.md": "---\ndescription: NovaMem stats snapshot (counts + last decay)\nallowed-tools: mcp__novamem__memory_stats\n---\n\nCall `mcp__novamem__memory_stats`. Render as a compact 4-line block:\nwarm / cold / graph counts, last decay run.\n",
      ".roo/commands/neighbors.md": "---\ndescription: Graph-neighbour traversal from a NovaMem entry id\nargument-hint: <seed-id> [depth=1] [k=10]\nallowed-tools: mcp__novamem__memory_neighbors\n---\n\nCall `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\nuser passed extra positional args, parse them as `depth` and `k` (both\nintegers). Default `depth: 1, k: 10`.\n\nTraversal runs over the server's SQL relations table; if that query\nfails, the response carries `degraded: true` — surface that briefly.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".roo/commands/project-create.md": "---\ndescription: Create a new NovaMem project (sub-brain)\nargument-hint: <id> <display-name>\nallowed-tools: mcp__novamem__project_create\n---\n\nParse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\nnew project record. After creation, future `/remember` and `/recall`\ncalls scoped to this id will land in the new sub-brain.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".roo/commands/projects.md": "---\ndescription: List NovaMem projects (sub-brains) you can access\nallowed-tools: mcp__novamem__project_list\n---\n\nCall `mcp__novamem__project_list`. Render each project as\n`id  name  role  members` on its own line. If there are zero, suggest\n`/project-create <id> <name>` to start a sub-brain.\n",
      ".roo/commands/recall.md": "---\ndescription: 5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\nargument-hint: <query>\nallowed-tools: mcp__novamem__memory_search\n---\n\nSearch NovaMem by calling `mcp__novamem__memory_search` with the query\nbelow. Use defaults unless the query strongly hints otherwise:\n\n- If the query is a literal id, symbol, or hash, pass\n  `weights: { keyword: 1, vector: 0 }`.\n- If the user asks for \"anything related to / similar to / like X\",\n  pass `weights: { vector: 1, keyword: 0 }`.\n\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\n1-line content excerpt. If nothing in the returned content actually answers the\nquery, say \"no strong matches\" and stop — don't pad with irrelevant hits. Don't\ngate on an absolute score: the usable range is model-dependent.\n\nQUERY:\n$ARGUMENTS\n",
      ".roo/commands/recent.md": "---\ndescription: Newest NovaMem entries (optionally in a namespace)\nargument-hint: [namespace]\nallowed-tools: mcp__novamem__memory_recent\n---\n\nCall `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\n`id  age  ns  one-line content`.\n",
      ".roo/commands/remember.md": "---\ndescription: Save a memory to NovaMem (durable across sessions)\nargument-hint: <content to remember>\nallowed-tools: mcp__novamem__memory_remember\n---\n\nStore the following as a NovaMem memory entry by calling\n`mcp__novamem__memory_remember`. If the user is currently working in a\nspecific project (visible in their recent context), pass `project: <id>`\nto scope it; otherwise leave project unset for a user-wide entry.\n\nAfter saving, reply in one short sentence with the assigned id and a\none-line summary of what was saved, so the user can edit or `/forget`\nif needed.\n\nCONTENT TO REMEMBER:\n$ARGUMENTS\n",
      ".roo/commands/today.md": "---\ndescription: NovaMem activity in the last 24h\nallowed-tools: mcp__novamem__memory_today\n---\n\nCall `mcp__novamem__memory_today` (no args). Render the result as a\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\nthere are more than 10 events.\n"
    }
  },
  "cline": {
    "tool": "cline",
    "files": {
      ".clinerules/workflows/forget.md": "---\ndescription: Permanently delete a NovaMem entry\nargument-hint: <entry-id>\nallowed-tools: mcp__novamem__memory_forget\n---\n\nCall `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\nthe warm row, the FTS index, the cold vector, and graph edges — it is\nnot recoverable. Confirm in one short line whether deletion succeeded.\n",
      ".clinerules/workflows/memory-stats.md": "---\ndescription: NovaMem stats snapshot (counts + last decay)\nallowed-tools: mcp__novamem__memory_stats\n---\n\nCall `mcp__novamem__memory_stats`. Render as a compact 4-line block:\nwarm / cold / graph counts, last decay run.\n",
      ".clinerules/workflows/neighbors.md": "---\ndescription: Graph-neighbour traversal from a NovaMem entry id\nargument-hint: <seed-id> [depth=1] [k=10]\nallowed-tools: mcp__novamem__memory_neighbors\n---\n\nCall `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\nuser passed extra positional args, parse them as `depth` and `k` (both\nintegers). Default `depth: 1, k: 10`.\n\nTraversal runs over the server's SQL relations table; if that query\nfails, the response carries `degraded: true` — surface that briefly.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".clinerules/workflows/project-create.md": "---\ndescription: Create a new NovaMem project (sub-brain)\nargument-hint: <id> <display-name>\nallowed-tools: mcp__novamem__project_create\n---\n\nParse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\nnew project record. After creation, future `/remember` and `/recall`\ncalls scoped to this id will land in the new sub-brain.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".clinerules/workflows/projects.md": "---\ndescription: List NovaMem projects (sub-brains) you can access\nallowed-tools: mcp__novamem__project_list\n---\n\nCall `mcp__novamem__project_list`. Render each project as\n`id  name  role  members` on its own line. If there are zero, suggest\n`/project-create <id> <name>` to start a sub-brain.\n",
      ".clinerules/workflows/recall.md": "---\ndescription: 5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\nargument-hint: <query>\nallowed-tools: mcp__novamem__memory_search\n---\n\nSearch NovaMem by calling `mcp__novamem__memory_search` with the query\nbelow. Use defaults unless the query strongly hints otherwise:\n\n- If the query is a literal id, symbol, or hash, pass\n  `weights: { keyword: 1, vector: 0 }`.\n- If the user asks for \"anything related to / similar to / like X\",\n  pass `weights: { vector: 1, keyword: 0 }`.\n\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\n1-line content excerpt. If nothing in the returned content actually answers the\nquery, say \"no strong matches\" and stop — don't pad with irrelevant hits. Don't\ngate on an absolute score: the usable range is model-dependent.\n\nQUERY:\n$ARGUMENTS\n",
      ".clinerules/workflows/recent.md": "---\ndescription: Newest NovaMem entries (optionally in a namespace)\nargument-hint: [namespace]\nallowed-tools: mcp__novamem__memory_recent\n---\n\nCall `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\n`id  age  ns  one-line content`.\n",
      ".clinerules/workflows/remember.md": "---\ndescription: Save a memory to NovaMem (durable across sessions)\nargument-hint: <content to remember>\nallowed-tools: mcp__novamem__memory_remember\n---\n\nStore the following as a NovaMem memory entry by calling\n`mcp__novamem__memory_remember`. If the user is currently working in a\nspecific project (visible in their recent context), pass `project: <id>`\nto scope it; otherwise leave project unset for a user-wide entry.\n\nAfter saving, reply in one short sentence with the assigned id and a\none-line summary of what was saved, so the user can edit or `/forget`\nif needed.\n\nCONTENT TO REMEMBER:\n$ARGUMENTS\n",
      ".clinerules/workflows/today.md": "---\ndescription: NovaMem activity in the last 24h\nallowed-tools: mcp__novamem__memory_today\n---\n\nCall `mcp__novamem__memory_today` (no args). Render the result as a\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\nthere are more than 10 events.\n"
    }
  },
  "continue": {
    "tool": "continue",
    "files": {
      ".continue/prompts/forget.prompt": "Permanently delete a NovaMem entry\n\nCall `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\nthe warm row, the FTS index, the cold vector, and graph edges — it is\nnot recoverable. Confirm in one short line whether deletion succeeded.\n",
      ".continue/prompts/memory-stats.prompt": "NovaMem stats snapshot (counts + last decay)\n\nCall `mcp__novamem__memory_stats`. Render as a compact 4-line block:\nwarm / cold / graph counts, last decay run.\n",
      ".continue/prompts/neighbors.prompt": "Graph-neighbour traversal from a NovaMem entry id\n\nCall `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\nuser passed extra positional args, parse them as `depth` and `k` (both\nintegers). Default `depth: 1, k: 10`.\n\nTraversal runs over the server's SQL relations table; if that query\nfails, the response carries `degraded: true` — surface that briefly.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".continue/prompts/project-create.prompt": "Create a new NovaMem project (sub-brain)\n\nParse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\nnew project record. After creation, future `/remember` and `/recall`\ncalls scoped to this id will land in the new sub-brain.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".continue/prompts/projects.prompt": "List NovaMem projects (sub-brains) you can access\n\nCall `mcp__novamem__project_list`. Render each project as\n`id  name  role  members` on its own line. If there are zero, suggest\n`/project-create <id> <name>` to start a sub-brain.\n",
      ".continue/prompts/recall.prompt": "5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\n\nSearch NovaMem by calling `mcp__novamem__memory_search` with the query\nbelow. Use defaults unless the query strongly hints otherwise:\n\n- If the query is a literal id, symbol, or hash, pass\n  `weights: { keyword: 1, vector: 0 }`.\n- If the user asks for \"anything related to / similar to / like X\",\n  pass `weights: { vector: 1, keyword: 0 }`.\n\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\n1-line content excerpt. If nothing in the returned content actually answers the\nquery, say \"no strong matches\" and stop — don't pad with irrelevant hits. Don't\ngate on an absolute score: the usable range is model-dependent.\n\nQUERY:\n$ARGUMENTS\n",
      ".continue/prompts/recent.prompt": "Newest NovaMem entries (optionally in a namespace)\n\nCall `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\n`id  age  ns  one-line content`.\n",
      ".continue/prompts/remember.prompt": "Save a memory to NovaMem (durable across sessions)\n\nStore the following as a NovaMem memory entry by calling\n`mcp__novamem__memory_remember`. If the user is currently working in a\nspecific project (visible in their recent context), pass `project: <id>`\nto scope it; otherwise leave project unset for a user-wide entry.\n\nAfter saving, reply in one short sentence with the assigned id and a\none-line summary of what was saved, so the user can edit or `/forget`\nif needed.\n\nCONTENT TO REMEMBER:\n$ARGUMENTS\n",
      ".continue/prompts/today.prompt": "NovaMem activity in the last 24h\n\nCall `mcp__novamem__memory_today` (no args). Render the result as a\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\nthere are more than 10 events.\n"
    }
  },
  "github-copilot": {
    "tool": "github-copilot",
    "files": {
      ".github/prompts/forget.prompt.md": "---\ndescription: Permanently delete a NovaMem entry\nargument-hint: <entry-id>\nallowed-tools: mcp__novamem__memory_forget\n---\n\nCall `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\nthe warm row, the FTS index, the cold vector, and graph edges — it is\nnot recoverable. Confirm in one short line whether deletion succeeded.\n",
      ".github/prompts/memory-stats.prompt.md": "---\ndescription: NovaMem stats snapshot (counts + last decay)\nallowed-tools: mcp__novamem__memory_stats\n---\n\nCall `mcp__novamem__memory_stats`. Render as a compact 4-line block:\nwarm / cold / graph counts, last decay run.\n",
      ".github/prompts/neighbors.prompt.md": "---\ndescription: Graph-neighbour traversal from a NovaMem entry id\nargument-hint: <seed-id> [depth=1] [k=10]\nallowed-tools: mcp__novamem__memory_neighbors\n---\n\nCall `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\nuser passed extra positional args, parse them as `depth` and `k` (both\nintegers). Default `depth: 1, k: 10`.\n\nTraversal runs over the server's SQL relations table; if that query\nfails, the response carries `degraded: true` — surface that briefly.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".github/prompts/project-create.prompt.md": "---\ndescription: Create a new NovaMem project (sub-brain)\nargument-hint: <id> <display-name>\nallowed-tools: mcp__novamem__project_create\n---\n\nParse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\nnew project record. After creation, future `/remember` and `/recall`\ncalls scoped to this id will land in the new sub-brain.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".github/prompts/projects.prompt.md": "---\ndescription: List NovaMem projects (sub-brains) you can access\nallowed-tools: mcp__novamem__project_list\n---\n\nCall `mcp__novamem__project_list`. Render each project as\n`id  name  role  members` on its own line. If there are zero, suggest\n`/project-create <id> <name>` to start a sub-brain.\n",
      ".github/prompts/recall.prompt.md": "---\ndescription: 5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\nargument-hint: <query>\nallowed-tools: mcp__novamem__memory_search\n---\n\nSearch NovaMem by calling `mcp__novamem__memory_search` with the query\nbelow. Use defaults unless the query strongly hints otherwise:\n\n- If the query is a literal id, symbol, or hash, pass\n  `weights: { keyword: 1, vector: 0 }`.\n- If the user asks for \"anything related to / similar to / like X\",\n  pass `weights: { vector: 1, keyword: 0 }`.\n\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\n1-line content excerpt. If nothing in the returned content actually answers the\nquery, say \"no strong matches\" and stop — don't pad with irrelevant hits. Don't\ngate on an absolute score: the usable range is model-dependent.\n\nQUERY:\n$ARGUMENTS\n",
      ".github/prompts/recent.prompt.md": "---\ndescription: Newest NovaMem entries (optionally in a namespace)\nargument-hint: [namespace]\nallowed-tools: mcp__novamem__memory_recent\n---\n\nCall `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\n`id  age  ns  one-line content`.\n",
      ".github/prompts/remember.prompt.md": "---\ndescription: Save a memory to NovaMem (durable across sessions)\nargument-hint: <content to remember>\nallowed-tools: mcp__novamem__memory_remember\n---\n\nStore the following as a NovaMem memory entry by calling\n`mcp__novamem__memory_remember`. If the user is currently working in a\nspecific project (visible in their recent context), pass `project: <id>`\nto scope it; otherwise leave project unset for a user-wide entry.\n\nAfter saving, reply in one short sentence with the assigned id and a\none-line summary of what was saved, so the user can edit or `/forget`\nif needed.\n\nCONTENT TO REMEMBER:\n$ARGUMENTS\n",
      ".github/prompts/today.prompt.md": "---\ndescription: NovaMem activity in the last 24h\nallowed-tools: mcp__novamem__memory_today\n---\n\nCall `mcp__novamem__memory_today` (no args). Render the result as a\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\nthere are more than 10 events.\n",
      ".mcp.json": "{\n  \"mcpServers\": {\n    \"other\": {\n      \"type\": \"sse\",\n      \"url\": \"https://other\"\n    },\n    \"novamem\": {\n      \"type\": \"sse\",\n      \"url\": \"https://memory.example.com/mcp/sse\",\n      \"headers\": {\n        \"Authorization\": \"Bearer nm_GOLDENFIXTURETOKEN\"\n      }\n    }\n  },\n  \"editorSetting\": 42\n}\n"
    }
  },
  "gemini": {
    "tool": "gemini",
    "files": {
      ".gemini/commands/forget.toml": "prompt = \"Call `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\\nthe warm row, the FTS index, the cold vector, and graph edges — it is\\nnot recoverable. Confirm in one short line whether deletion succeeded.\"\ndescription = \"Permanently delete a NovaMem entry\"\n\n",
      ".gemini/commands/memory-stats.toml": "prompt = \"Call `mcp__novamem__memory_stats`. Render as a compact 4-line block:\\nwarm / cold / graph counts, last decay run.\"\ndescription = \"NovaMem stats snapshot (counts + last decay)\"\n\n",
      ".gemini/commands/neighbors.toml": "prompt = \"Call `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\\nuser passed extra positional args, parse them as `depth` and `k` (both\\nintegers). Default `depth: 1, k: 10`.\\n\\nTraversal runs over the server's SQL relations table; if that query\\nfails, the response carries `degraded: true` — surface that briefly.\\n\\nARGUMENTS:\\n$ARGUMENTS\"\ndescription = \"Graph-neighbour traversal from a NovaMem entry id\"\n\n",
      ".gemini/commands/project-create.toml": "prompt = \"Parse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\\nnew project record. After creation, future `/remember` and `/recall`\\ncalls scoped to this id will land in the new sub-brain.\\n\\nARGUMENTS:\\n$ARGUMENTS\"\ndescription = \"Create a new NovaMem project (sub-brain)\"\n\n",
      ".gemini/commands/projects.toml": "prompt = \"Call `mcp__novamem__project_list`. Render each project as\\n`id  name  role  members` on its own line. If there are zero, suggest\\n`/project-create <id> <name>` to start a sub-brain.\"\ndescription = \"List NovaMem projects (sub-brains) you can access\"\n\n",
      ".gemini/commands/recall.toml": "prompt = \"Search NovaMem by calling `mcp__novamem__memory_search` with the query\\nbelow. Use defaults unless the query strongly hints otherwise:\\n\\n- If the query is a literal id, symbol, or hash, pass\\n  `weights: { keyword: 1, vector: 0 }`.\\n- If the user asks for \\\"anything related to / similar to / like X\\\",\\n  pass `weights: { vector: 1, keyword: 0 }`.\\n\\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\\n1-line content excerpt. If nothing in the returned content actually answers the\\nquery, say \\\"no strong matches\\\" and stop — don't pad with irrelevant hits. Don't\\ngate on an absolute score: the usable range is model-dependent.\\n\\nQUERY:\\n$ARGUMENTS\"\ndescription = \"5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\"\n\n",
      ".gemini/commands/recent.toml": "prompt = \"Call `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\\n`id  age  ns  one-line content`.\"\ndescription = \"Newest NovaMem entries (optionally in a namespace)\"\n\n",
      ".gemini/commands/remember.toml": "prompt = \"Store the following as a NovaMem memory entry by calling\\n`mcp__novamem__memory_remember`. If the user is currently working in a\\nspecific project (visible in their recent context), pass `project: <id>`\\nto scope it; otherwise leave project unset for a user-wide entry.\\n\\nAfter saving, reply in one short sentence with the assigned id and a\\none-line summary of what was saved, so the user can edit or `/forget`\\nif needed.\\n\\nCONTENT TO REMEMBER:\\n$ARGUMENTS\"\ndescription = \"Save a memory to NovaMem (durable across sessions)\"\n\n",
      ".gemini/commands/today.toml": "prompt = \"Call `mcp__novamem__memory_today` (no args). Render the result as a\\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\\nthere are more than 10 events.\"\ndescription = \"NovaMem activity in the last 24h\"\n\n",
      ".gemini/settings.json": "{\n  \"mcpServers\": {\n    \"other\": {\n      \"type\": \"sse\",\n      \"url\": \"https://other\"\n    },\n    \"novamem\": {\n      \"command\": \"npx\",\n      \"args\": [\n        \"-y\",\n        \"@azrtydxb/novamem-mcp@9.9.9\"\n      ],\n      \"env\": {\n        \"NOVAMEM_BASE_URL\": \"https://memory.example.com\",\n        \"NOVAMEM_TOKEN\": \"nm_GOLDENFIXTURETOKEN\"\n      }\n    }\n  },\n  \"editorSetting\": 42\n}\n"
    }
  },
  "codex": {
    "tool": "codex",
    "files": {
      ".codex/config.toml": "[mcp_servers.novamem]\ncommand = \"npx\"\nargs = [ \"-y\", \"@azrtydxb/novamem-mcp@9.9.9\" ]\n\n[mcp_servers.novamem.env]\nNOVAMEM_BASE_URL = \"https://memory.example.com\"\nNOVAMEM_TOKEN = \"nm_GOLDENFIXTURETOKEN\"\n\n",
      ".codex/prompts/memory-forget.md": "---\ndescription: Permanently delete a NovaMem entry\nargument-hint: <entry-id>\nallowed-tools: mcp__novamem__memory_forget\n---\n\nCall `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\nthe warm row, the FTS index, the cold vector, and graph edges — it is\nnot recoverable. Confirm in one short line whether deletion succeeded.\n",
      ".codex/prompts/memory-memory-stats.md": "---\ndescription: NovaMem stats snapshot (counts + last decay)\nallowed-tools: mcp__novamem__memory_stats\n---\n\nCall `mcp__novamem__memory_stats`. Render as a compact 4-line block:\nwarm / cold / graph counts, last decay run.\n",
      ".codex/prompts/memory-neighbors.md": "---\ndescription: Graph-neighbour traversal from a NovaMem entry id\nargument-hint: <seed-id> [depth=1] [k=10]\nallowed-tools: mcp__novamem__memory_neighbors\n---\n\nCall `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\nuser passed extra positional args, parse them as `depth` and `k` (both\nintegers). Default `depth: 1, k: 10`.\n\nTraversal runs over the server's SQL relations table; if that query\nfails, the response carries `degraded: true` — surface that briefly.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".codex/prompts/memory-project-create.md": "---\ndescription: Create a new NovaMem project (sub-brain)\nargument-hint: <id> <display-name>\nallowed-tools: mcp__novamem__project_create\n---\n\nParse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\nnew project record. After creation, future `/remember` and `/recall`\ncalls scoped to this id will land in the new sub-brain.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".codex/prompts/memory-projects.md": "---\ndescription: List NovaMem projects (sub-brains) you can access\nallowed-tools: mcp__novamem__project_list\n---\n\nCall `mcp__novamem__project_list`. Render each project as\n`id  name  role  members` on its own line. If there are zero, suggest\n`/project-create <id> <name>` to start a sub-brain.\n",
      ".codex/prompts/memory-recall.md": "---\ndescription: 5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\nargument-hint: <query>\nallowed-tools: mcp__novamem__memory_search\n---\n\nSearch NovaMem by calling `mcp__novamem__memory_search` with the query\nbelow. Use defaults unless the query strongly hints otherwise:\n\n- If the query is a literal id, symbol, or hash, pass\n  `weights: { keyword: 1, vector: 0 }`.\n- If the user asks for \"anything related to / similar to / like X\",\n  pass `weights: { vector: 1, keyword: 0 }`.\n\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\n1-line content excerpt. If nothing in the returned content actually answers the\nquery, say \"no strong matches\" and stop — don't pad with irrelevant hits. Don't\ngate on an absolute score: the usable range is model-dependent.\n\nQUERY:\n$ARGUMENTS\n",
      ".codex/prompts/memory-recent.md": "---\ndescription: Newest NovaMem entries (optionally in a namespace)\nargument-hint: [namespace]\nallowed-tools: mcp__novamem__memory_recent\n---\n\nCall `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\n`id  age  ns  one-line content`.\n",
      ".codex/prompts/memory-remember.md": "---\ndescription: Save a memory to NovaMem (durable across sessions)\nargument-hint: <content to remember>\nallowed-tools: mcp__novamem__memory_remember\n---\n\nStore the following as a NovaMem memory entry by calling\n`mcp__novamem__memory_remember`. If the user is currently working in a\nspecific project (visible in their recent context), pass `project: <id>`\nto scope it; otherwise leave project unset for a user-wide entry.\n\nAfter saving, reply in one short sentence with the assigned id and a\none-line summary of what was saved, so the user can edit or `/forget`\nif needed.\n\nCONTENT TO REMEMBER:\n$ARGUMENTS\n",
      ".codex/prompts/memory-today.md": "---\ndescription: NovaMem activity in the last 24h\nallowed-tools: mcp__novamem__memory_today\n---\n\nCall `mcp__novamem__memory_today` (no args). Render the result as a\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\nthere are more than 10 events.\n"
    }
  },
  "factory": {
    "tool": "factory",
    "files": {
      ".factory/commands/forget.md": "---\ndescription: Permanently delete a NovaMem entry\nargument-hint: <entry-id>\nallowed-tools: mcp__novamem__memory_forget\n---\n\nCall `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\nthe warm row, the FTS index, the cold vector, and graph edges — it is\nnot recoverable. Confirm in one short line whether deletion succeeded.\n",
      ".factory/commands/memory-stats.md": "---\ndescription: NovaMem stats snapshot (counts + last decay)\nallowed-tools: mcp__novamem__memory_stats\n---\n\nCall `mcp__novamem__memory_stats`. Render as a compact 4-line block:\nwarm / cold / graph counts, last decay run.\n",
      ".factory/commands/neighbors.md": "---\ndescription: Graph-neighbour traversal from a NovaMem entry id\nargument-hint: <seed-id> [depth=1] [k=10]\nallowed-tools: mcp__novamem__memory_neighbors\n---\n\nCall `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\nuser passed extra positional args, parse them as `depth` and `k` (both\nintegers). Default `depth: 1, k: 10`.\n\nTraversal runs over the server's SQL relations table; if that query\nfails, the response carries `degraded: true` — surface that briefly.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".factory/commands/project-create.md": "---\ndescription: Create a new NovaMem project (sub-brain)\nargument-hint: <id> <display-name>\nallowed-tools: mcp__novamem__project_create\n---\n\nParse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\nnew project record. After creation, future `/remember` and `/recall`\ncalls scoped to this id will land in the new sub-brain.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".factory/commands/projects.md": "---\ndescription: List NovaMem projects (sub-brains) you can access\nallowed-tools: mcp__novamem__project_list\n---\n\nCall `mcp__novamem__project_list`. Render each project as\n`id  name  role  members` on its own line. If there are zero, suggest\n`/project-create <id> <name>` to start a sub-brain.\n",
      ".factory/commands/recall.md": "---\ndescription: 5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\nargument-hint: <query>\nallowed-tools: mcp__novamem__memory_search\n---\n\nSearch NovaMem by calling `mcp__novamem__memory_search` with the query\nbelow. Use defaults unless the query strongly hints otherwise:\n\n- If the query is a literal id, symbol, or hash, pass\n  `weights: { keyword: 1, vector: 0 }`.\n- If the user asks for \"anything related to / similar to / like X\",\n  pass `weights: { vector: 1, keyword: 0 }`.\n\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\n1-line content excerpt. If nothing in the returned content actually answers the\nquery, say \"no strong matches\" and stop — don't pad with irrelevant hits. Don't\ngate on an absolute score: the usable range is model-dependent.\n\nQUERY:\n$ARGUMENTS\n",
      ".factory/commands/recent.md": "---\ndescription: Newest NovaMem entries (optionally in a namespace)\nargument-hint: [namespace]\nallowed-tools: mcp__novamem__memory_recent\n---\n\nCall `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\n`id  age  ns  one-line content`.\n",
      ".factory/commands/remember.md": "---\ndescription: Save a memory to NovaMem (durable across sessions)\nargument-hint: <content to remember>\nallowed-tools: mcp__novamem__memory_remember\n---\n\nStore the following as a NovaMem memory entry by calling\n`mcp__novamem__memory_remember`. If the user is currently working in a\nspecific project (visible in their recent context), pass `project: <id>`\nto scope it; otherwise leave project unset for a user-wide entry.\n\nAfter saving, reply in one short sentence with the assigned id and a\none-line summary of what was saved, so the user can edit or `/forget`\nif needed.\n\nCONTENT TO REMEMBER:\n$ARGUMENTS\n",
      ".factory/commands/today.md": "---\ndescription: NovaMem activity in the last 24h\nallowed-tools: mcp__novamem__memory_today\n---\n\nCall `mcp__novamem__memory_today` (no args). Render the result as a\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\nthere are more than 10 events.\n"
    }
  },
  "windsurf": {
    "tool": "windsurf",
    "files": {
      ".windsurf/workflows/forget.md": "---\ndescription: Permanently delete a NovaMem entry\nargument-hint: <entry-id>\nallowed-tools: mcp__novamem__memory_forget\n---\n\nCall `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\nthe warm row, the FTS index, the cold vector, and graph edges — it is\nnot recoverable. Confirm in one short line whether deletion succeeded.\n",
      ".windsurf/workflows/memory-stats.md": "---\ndescription: NovaMem stats snapshot (counts + last decay)\nallowed-tools: mcp__novamem__memory_stats\n---\n\nCall `mcp__novamem__memory_stats`. Render as a compact 4-line block:\nwarm / cold / graph counts, last decay run.\n",
      ".windsurf/workflows/neighbors.md": "---\ndescription: Graph-neighbour traversal from a NovaMem entry id\nargument-hint: <seed-id> [depth=1] [k=10]\nallowed-tools: mcp__novamem__memory_neighbors\n---\n\nCall `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\nuser passed extra positional args, parse them as `depth` and `k` (both\nintegers). Default `depth: 1, k: 10`.\n\nTraversal runs over the server's SQL relations table; if that query\nfails, the response carries `degraded: true` — surface that briefly.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".windsurf/workflows/project-create.md": "---\ndescription: Create a new NovaMem project (sub-brain)\nargument-hint: <id> <display-name>\nallowed-tools: mcp__novamem__project_create\n---\n\nParse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\nnew project record. After creation, future `/remember` and `/recall`\ncalls scoped to this id will land in the new sub-brain.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".windsurf/workflows/projects.md": "---\ndescription: List NovaMem projects (sub-brains) you can access\nallowed-tools: mcp__novamem__project_list\n---\n\nCall `mcp__novamem__project_list`. Render each project as\n`id  name  role  members` on its own line. If there are zero, suggest\n`/project-create <id> <name>` to start a sub-brain.\n",
      ".windsurf/workflows/recall.md": "---\ndescription: 5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\nargument-hint: <query>\nallowed-tools: mcp__novamem__memory_search\n---\n\nSearch NovaMem by calling `mcp__novamem__memory_search` with the query\nbelow. Use defaults unless the query strongly hints otherwise:\n\n- If the query is a literal id, symbol, or hash, pass\n  `weights: { keyword: 1, vector: 0 }`.\n- If the user asks for \"anything related to / similar to / like X\",\n  pass `weights: { vector: 1, keyword: 0 }`.\n\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\n1-line content excerpt. If nothing in the returned content actually answers the\nquery, say \"no strong matches\" and stop — don't pad with irrelevant hits. Don't\ngate on an absolute score: the usable range is model-dependent.\n\nQUERY:\n$ARGUMENTS\n",
      ".windsurf/workflows/recent.md": "---\ndescription: Newest NovaMem entries (optionally in a namespace)\nargument-hint: [namespace]\nallowed-tools: mcp__novamem__memory_recent\n---\n\nCall `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\n`id  age  ns  one-line content`.\n",
      ".windsurf/workflows/remember.md": "---\ndescription: Save a memory to NovaMem (durable across sessions)\nargument-hint: <content to remember>\nallowed-tools: mcp__novamem__memory_remember\n---\n\nStore the following as a NovaMem memory entry by calling\n`mcp__novamem__memory_remember`. If the user is currently working in a\nspecific project (visible in their recent context), pass `project: <id>`\nto scope it; otherwise leave project unset for a user-wide entry.\n\nAfter saving, reply in one short sentence with the assigned id and a\none-line summary of what was saved, so the user can edit or `/forget`\nif needed.\n\nCONTENT TO REMEMBER:\n$ARGUMENTS\n",
      ".windsurf/workflows/today.md": "---\ndescription: NovaMem activity in the last 24h\nallowed-tools: mcp__novamem__memory_today\n---\n\nCall `mcp__novamem__memory_today` (no args). Render the result as a\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\nthere are more than 10 events.\n"
    }
  },
  "amazon-q": {
    "tool": "amazon-q",
    "files": {
      ".amazonq/prompts/memory-forget.md": "---\ndescription: Permanently delete a NovaMem entry\nargument-hint: <entry-id>\nallowed-tools: mcp__novamem__memory_forget\n---\n\nCall `mcp__novamem__memory_forget` with `id: $ARGUMENTS`. Forget removes\nthe warm row, the FTS index, the cold vector, and graph edges — it is\nnot recoverable. Confirm in one short line whether deletion succeeded.\n",
      ".amazonq/prompts/memory-memory-stats.md": "---\ndescription: NovaMem stats snapshot (counts + last decay)\nallowed-tools: mcp__novamem__memory_stats\n---\n\nCall `mcp__novamem__memory_stats`. Render as a compact 4-line block:\nwarm / cold / graph counts, last decay run.\n",
      ".amazonq/prompts/memory-neighbors.md": "---\ndescription: Graph-neighbour traversal from a NovaMem entry id\nargument-hint: <seed-id> [depth=1] [k=10]\nallowed-tools: mcp__novamem__memory_neighbors\n---\n\nCall `mcp__novamem__memory_neighbors` with `id: <first arg>`. If the\nuser passed extra positional args, parse them as `depth` and `k` (both\nintegers). Default `depth: 1, k: 10`.\n\nTraversal runs over the server's SQL relations table; if that query\nfails, the response carries `degraded: true` — surface that briefly.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".amazonq/prompts/memory-project-create.md": "---\ndescription: Create a new NovaMem project (sub-brain)\nargument-hint: <id> <display-name>\nallowed-tools: mcp__novamem__project_create\n---\n\nParse `$ARGUMENTS` as `<id> <display name…>`. The id must be a URL-safe\nslug, 2–64 chars, alphanumeric / dot / underscore / dash. Call\n`mcp__novamem__project_create` with `{ id, name }` and reply with the\nnew project record. After creation, future `/remember` and `/recall`\ncalls scoped to this id will land in the new sub-brain.\n\nARGUMENTS:\n$ARGUMENTS\n",
      ".amazonq/prompts/memory-projects.md": "---\ndescription: List NovaMem projects (sub-brains) you can access\nallowed-tools: mcp__novamem__project_list\n---\n\nCall `mcp__novamem__project_list`. Render each project as\n`id  name  role  members` on its own line. If there are zero, suggest\n`/project-create <id> <name>` to start a sub-brain.\n",
      ".amazonq/prompts/memory-recall.md": "---\ndescription: 5-signal hybrid retrieval (keyword + vector + graph + recency + entity, graph/entity at 0 in production calibration)\nargument-hint: <query>\nallowed-tools: mcp__novamem__memory_search\n---\n\nSearch NovaMem by calling `mcp__novamem__memory_search` with the query\nbelow. Use defaults unless the query strongly hints otherwise:\n\n- If the query is a literal id, symbol, or hash, pass\n  `weights: { keyword: 1, vector: 0 }`.\n- If the user asks for \"anything related to / similar to / like X\",\n  pass `weights: { vector: 1, keyword: 0 }`.\n\nCap at `k: 10`. Show results compactly: id (short hash), score, tier,\n1-line content excerpt. If nothing in the returned content actually answers the\nquery, say \"no strong matches\" and stop — don't pad with irrelevant hits. Don't\ngate on an absolute score: the usable range is model-dependent.\n\nQUERY:\n$ARGUMENTS\n",
      ".amazonq/prompts/memory-recent.md": "---\ndescription: Newest NovaMem entries (optionally in a namespace)\nargument-hint: [namespace]\nallowed-tools: mcp__novamem__memory_recent\n---\n\nCall `mcp__novamem__memory_recent` with `k: 20` and, if an argument was\nsupplied, `namespace: $ARGUMENTS`. Render newest first as\n`id  age  ns  one-line content`.\n",
      ".amazonq/prompts/memory-remember.md": "---\ndescription: Save a memory to NovaMem (durable across sessions)\nargument-hint: <content to remember>\nallowed-tools: mcp__novamem__memory_remember\n---\n\nStore the following as a NovaMem memory entry by calling\n`mcp__novamem__memory_remember`. If the user is currently working in a\nspecific project (visible in their recent context), pass `project: <id>`\nto scope it; otherwise leave project unset for a user-wide entry.\n\nAfter saving, reply in one short sentence with the assigned id and a\none-line summary of what was saved, so the user can edit or `/forget`\nif needed.\n\nCONTENT TO REMEMBER:\n$ARGUMENTS\n",
      ".amazonq/prompts/memory-today.md": "---\ndescription: NovaMem activity in the last 24h\nallowed-tools: mcp__novamem__memory_today\n---\n\nCall `mcp__novamem__memory_today` (no args). Render the result as a\ncompact timeline: `HH:MM  KIND  text`, newest first. Group by kind if\nthere are more than 10 events.\n"
    }
  },
  "antigravity": {
    "tool": "antigravity",
    "files": {}
  },
  "auggie": {
    "tool": "auggie",
    "files": {}
  },
  "bob": {
    "tool": "bob",
    "files": {}
  },
  "codebuddy": {
    "tool": "codebuddy",
    "files": {}
  },
  "costrict": {
    "tool": "costrict",
    "files": {}
  },
  "crush": {
    "tool": "crush",
    "files": {}
  },
  "forgecode": {
    "tool": "forgecode",
    "files": {}
  },
  "iflow": {
    "tool": "iflow",
    "files": {}
  },
  "junie": {
    "tool": "junie",
    "files": {}
  },
  "kimi": {
    "tool": "kimi",
    "files": {}
  },
  "kiro": {
    "tool": "kiro",
    "files": {}
  },
  "lingma": {
    "tool": "lingma",
    "files": {}
  },
  "pi": {
    "tool": "pi",
    "files": {}
  },
  "qoder": {
    "tool": "qoder",
    "files": {}
  },
  "qwen": {
    "tool": "qwen",
    "files": {}
  },
  "trae": {
    "tool": "trae",
    "files": {}
  }
}
