---
description: NovaMem stats snapshot (counts + last decay)
allowed-tools: mcp__novamem__memory_stats
---

Call `mcp__novamem__memory.stats`. Render as a compact 4-line block:
warm / cold / graph counts, last decay run.
