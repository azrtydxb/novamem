---
description: List NovaMem projects (sub-brains) you can access
allowed-tools: mcp__novamem__project_list
---

Call `mcp__novamem__project.list`. Render each project as
`id  name  role  members` on its own line. If there are zero, suggest
`/project-create <id> <name>` to start a sub-brain.
