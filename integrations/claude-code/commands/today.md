---
description: NovaMem activity in the last 24h
allowed-tools: mcp__novamem__memory_today
---

Call `mcp__novamem__memory.today` (no args). Render the result as a
compact timeline: `HH:MM  KIND  text`, newest first. Group by kind if
there are more than 10 events.
