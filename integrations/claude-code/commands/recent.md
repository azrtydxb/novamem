---
description: Newest NovaMem entries (optionally in a namespace)
argument-hint: [namespace]
allowed-tools: mcp__novamem__memory_recent
---

Call `mcp__novamem__memory_recent` with `k: 20` and, if an argument was
supplied, `namespace: $ARGUMENTS`. Render newest first as
`id  age  ns  one-line content`.
