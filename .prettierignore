# Generated files — never hand-formatted.
# Everything below is written by `go run ./cmd/gen-contract` from
# api/openapi.yaml, which IS formatted because it is the authored source.
# Formatting an output would put prettier and the generator in a loop:
# each run would undo the other and the CI drift gate would report a
# change on every build.
docs/api/openapi.json
go/internal/httpapi/openapi.json
go/internal/mcp/tooldefs.json
# Vite build output synced from packages/admin-ui and embedded in the Go
# binary (go/scripts/sync-admin-ui.sh).
go/internal/httpapi/admin-ui/
# Hand-tuned static marketing pages: prettier's HTML reflow changes
# rendered whitespace inside inline elements (verified in a browser).
site/*.html
# Written by pnpm; prettier and the package manager would fight over it
# on every install.
pnpm-lock.yaml
# Captured oracles: these are byte-for-byte what a retired implementation
# produced, kept as evidence for the port that replaced it. Reformatting
# them would mean the fixture is no longer literally what was observed.
go/internal/bench/testdata/*.golden.json
go/internal/initcli/testdata/golden.json
