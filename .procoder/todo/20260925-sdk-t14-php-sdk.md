# SDK T14: PHP SDK

Status: open
Created: 2026-09-25

## Description

Ship clients/php (Packagist `azrtydxb/novamem` via the novamem-php mirror, ext-curl and ext-json only), held to the shared scenario suite (plan Task 14, spec S-11).

## Acceptance criteria

- [ ] `cd clients/php && vendor/bin/phpunit` passes on PHP 8.2 and 8.4
- [ ] `composer install --no-dev && composer show --no-dev` lists nothing
- [ ] Removing token redaction fails `token-echoed-in-401-is-redacted` (mutation check, reverted)
- [ ] smoke.php exists, and the `php` job is in sdk.yml

## Evidence
