# SDK T14: PHP SDK

Status: closed 2026-09-25
Created: 2026-09-25

## Description

Ship clients/php (Packagist `azrtydxb/novamem` via the novamem-php mirror, ext-curl and ext-json only), held to the shared scenario suite (plan Task 14, spec S-11).

## Acceptance criteria

- [x] `cd clients/php && vendor/bin/phpunit` passes on PHP 8.2 and 8.4 (8.5.6 locally; 8.2 and 8.4 in the sdk.yml `php` matrix)
- [x] `composer install --no-dev && composer show --no-dev` lists nothing
- [x] Removing token redaction fails `token-echoed-in-401-is-redacted` (mutation check, reverted)
- [x] smoke.php exists, and the `php` job is in sdk.yml

## Evidence

- Local (PHP 8.5.6): `cd clients/php && vendor/bin/phpunit` → `Tests: 90, Assertions: 341, Skipped: 2`. That covers 80 scenarios (the cancel pair skipped), routes, 3 redirect tests (cross-origin drop, other port, same-origin keep) and 5 wire tests (empty map as {}, required null, optional omission, timestamps, unknown keys).
- `composer install --no-dev` in a clean directory → `composer show --no-dev` prints nothing. phpstan (level 6, clients/php/phpstan.neon) → `[OK] No errors`.
- Mutation: disabling degradedEmpty → 1 failure. Removing the redaction → `token-echoed` fails. Keeping auth across origins → `the bearer followed a redirect to another origin`. All restored.
- smoke.php against a dead port: `PASS php down`, and `up` → `php capture: … Could not connect` with exit 1. The sdk.yml `php` job (phpunit + phpstan + no-dev check) exists; the gate is clean.
